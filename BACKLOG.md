# Backlog: тайловый/картографический виджет

Рабочий бэклог развития картографического виджета (`src/map.ts` + окружение).
Каждая задача — отдельный чек-лист-пункт с ID (для ссылок из коммитов/PR:
`git commit -m "AFF-2: ..."`), кратким описанием, критерием готовности,
зависимостями и грубой оценкой сложности (S/M/L/XL — не время, а объём риска
и работы).

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` сделано.

Новые идеи/вопросы сначала попадают в `INBOX.md` (быстрый нефильтрованный
список), а сюда — уже разобранными, с ID и оценкой. Пункты, требующие сначала
подумать/сформулировать концепцию, а не сразу кодировать, — в
[Открытые вопросы](#открытые-вопросы), не сразу тут в виде задач. Отдельно,
`DEMO_BACKLOG.md` — house-keeping тестовой демо-страницы (`src/index.ts`/
`src/layers.ts`), не самого виджета — граница та же, что формализует `INFRA-5`.

**Видение (зачем всё это).** Ориентир по функциональности —
[nakarte](https://github.com/trapwalker/nakarte) (см. `DEMO_BACKLOG.md`/
`DEMO-2` — разбор его слоёв/фич) и заметно дальше: конечная цель —
"гео-блокнот", инструмент для быстрых GIS-вычислений/построений/прикидок,
способный работать полностью офлайн (свой набор локально разворачиваемых
гео-сервисов — тайлы, геокодирование, адаптеры позиционирования). Это
big-picture ориентир для приоритизации, а не текущая фаза работы — конкретный
момент, когда офлайн-инфраструктура станет отдельной фазой бэклога, наступит
позже; пока каждое архитектурное решение ниже стоит сверять на "не мешает ли
это офлайн-геосервисам и гео-блокноту в будущем", даже не реализуя их сейчас.

Актуальное состояние ядра на момент написания бэклога зафиксировано в разделе
[Аудит текущего состояния](#аудит-текущего-состояния) — оттуда растут почти
все задачи ниже.

## Дорожная карта (порядок фаз)

1. **Фаза 0 — быстрые правки** на текущей архитектуре (не блокируют друг
   друга, дают немедленный эффект): `ZOOM-2`, `ZOOM-4`, `ZOOM-7`, `LOAD-1`.
2. **Фаза 1 — афинное ядро** (`AFF-*`): фундамент для всего, что связано с
   поворотом, вложенными вьюпортами и корректным zoom-to-cursor. Сразу
   вслед за `AFF-3` (который неожиданно рано, уже сейчас, перевёл весь
   `src/` на настоящие ES-модули — см. `AFF-3`) — `INFRA-5` (разделение
   `core`/`demo`), пока публичная поверхность API ещё небольшая: виджет
   задуман как отдельная переиспользуемая библиотека, и границу лучше
   провести на этом этапе, а не распутывать её постфактум.
3. **Фаза 2 — зум и панорамирование через афинный стек** (`ZOOM-1`, `ZOOM-3`,
   `ZOOM-5`, `ZOOM-6`, `ZOOM-8`) — переезжают на `AFF-*`.
4. **Фаза 3 (параллельно фазам 1-2) — источники тайлов** (`SRC-*`) —
   архитектурно почти независима от афинного ядра.
5. **Фаза 4 (параллельно) — фоновая загрузка/прогрев** (`LOAD-*`) —
   независима, но полезнее после `AFF-*`, т.к. прогрев должен знать про
   поворот вьюпорта.
6. **Фаза 5 — вращение** (`ROT-*`) — зависит от `AFF-*`.
7. **Фаза 6 — вложенные вьюпорты** (`VP-*`) — зависит от `AFF-*`, выигрывает
   от готового `ROT-*`.
8. **Фаза 7 — векторные слои** (`VEC-*`) — зависит от `AFF-*`; MVP можно
   начать раньше, если временно исключить поворот.

**Дальше — превращение прототипа в полноценный переиспользуемый
картографический виджет** (см. [Аудит: от прототипа к
виджету](#аудит-от-прототипа-к-виджету) и `INFRA-5`/`INFRA-6`, уже решённые
как обязательные — "виджет — отдельная библиотека"):

9. **Фаза 8 — событийная модель** (`EVT-*`) — сквозная инфраструктура,
   которую используют почти все фазы ниже (и на которую стоит перевести
   уже существующие `onLocate`/`onZoom`); архитектурно независима от
   `AFF-*`/`ROT-*`, можно делать параллельно с чем угодно.
10. **Фаза 9 — координаты и проекции** (`PROJ-*`) — независимый чистый
    модуль (как `Mat2D`), нужен раньше `DYN-*` и любой реальной
    геопривязки данных (GPS/GeoJSON в WGS84).
11. **Фаза 10 — композиция слоёв и оверлеи** (`LAYER-*`) — зависит от
    `AFF-4` (готово) и выигрывает от `EVT-*`.
12. **Фаза 7 (продолжение) — редактирование геометрии** (`VEC-8..VEC-10`)
    — зависит от `VEC-1..VEC-4` и `EVT-*`.
13. **Фаза 11 — сериализация состояния** (`STATE-*`) — зависит от `EVT-2`
    (события `moveend`/`zoomend`/`rotateend`), расширяет уже частично
    сделанное в `ROT-2`.
14. **Фаза 12 — плавные переходы вьюпорта** (`FLY-*`) — зависит от `EVT-*`;
    `STATE-*`'s восстановление из URL использует `FLY-4` (мгновенный
    переход).
15. **Фаза 13 — адаптивный размер виджета** (`RESIZE-*`) — архитектурно
    независима, можно в любой момент.
16. **Фаза 14 — динамические и движущиеся объекты** (`DYN-*`) — самая
    "верхнеуровневая" фаза, зависит от `VEC-*`, `PROJ-*`, `EVT-*`, `FLY-3`.
17. **Фаза 15 — закладки** (`BOOKMARK-*`) — независимый модуль (как
    `EventEmitter`), MVP не требует ничего из фаз выше; полноценно
    раскрывается вместе с `LAYER-*` (привязка закладки к слою) и `EVT-*`
    (события добавления/удаления).
18. **Сквозное — инфраструктура и качество** (`INFRA-*`) — по ходу всех фаз.

---

## Аудит текущего состояния

Зафиксированные наблюдения по коду (`src/map.ts`, `src/tile_source.ts`,
`src/tile_tree.ts`, `src/layers.ts`, `src/vector.ts`, `src/index.ts`),
на которые опираются задачи ниже:

- Зум крутится вокруг центра канваса всегда: обработчик `wheel` в
  `MapWidget` (`src/map.ts:109`) меняет только `zoom_target`, не трогая
  `this.c` — точка под курсором "уезжает". Двойной клик, наоборот, умеет
  центрировать под курсор (`src/map.ts:148`), т.е. в коде уже есть нужная
  формула, просто не применена к колесу. Всё ещё не исправлено, см.
  `ZOOM-1`.
- Анимация зума считалась **по кадрам**, не по времени:
  `zoom_factor += (zoom_target - zoom_factor) / zoom_animation_factor`
  зависело от FPS, хотя `dt_stat`/`this.t` уже считались рядом и не
  использовались для этого. **Исправлено в `ZOOM-2`** (сама анимация
  зума; панорамирование с инерцией — ещё нет, см. статус `ZOOM-2`).
- Второй канвас — был баг: `canvas2` создавался, но
  `this.ctx2 = this.canvas.getContext('2d')` — это же первый канвас,
  `canvas2` нигде не использовался. **Исправлено в `ZOOM-7`** — мёртвый
  код убран целиком.
- `devicePixelRatio` нигде не учитывается — на Retina/HiDPI картинка мылится
  (`onResize`, `src/map.ts:171`). Всё ещё не исправлено, см. `ZOOM-8`.
- Ввод — только `mousedown/mousemove/mouseup/wheel/keydown` (`src/map.ts`);
  тач/пинч не поддержаны вовсе. `e.keyCode` (107/109, deprecated) —
  **исправлено в `ZOOM-4`**, тач/пинч — **исправлено в `ZOOM-3`**.
- ~~`TiledLayer.getLevelParams`/`draw` считают позиции тайлов вручную в
  осевыровненных экранных координатах — заменить на единую матрицу
  трансформации будет означать переписывание этого метода почти с
  нуля~~ — **сделано в `AFF-4`**: позиционирование идёт через
  `computeTileGridMatrix`/`ctx.setTransform`; выбор диапазона видимых
  тайлов остался осевыровненным — **исправлено в `ROT-3-range`**.
- ~~`Layer.shift` объявлен в опциях, но `TiledLayer.draw` его не
  использует вообще — мёртвая функциональность~~ — **исправлено в
  `AFF-4`**: `shift` (плюс новые `scale`/`rotation`) читаются в
  `Layer.transform: Transform2D` при конструировании и реально влияют
  на отрисовку.
- ~~Прогрев тайлов (`TSCache.heat`, `src/tile_source.ts:60`) вызывается
  вручную только из `locations.*.go()` (`src/defines.ts`) для xkcd-слоя~~
  — **уточнение:** проверка кодовой базы (`grep -rn '\.heat('`) показала,
  что до `LOAD-1` метод не вызывался вообще нигде, даже из
  `locations.*.go()`; штатный путь через `onLocate` в `index.ts:15-18`
  был **закомментирован**. Т.е. предзагрузка была полностью отключена от
  обычного панорамирования/зума карты. **Исправлено в `LOAD-1`** — см.
  "Принятые решения"/статус задачи ниже.
- `load_queue` в `TSCache` — это стек (`Array.pop()`,
  `src/tile_source.ts:52`), а не приоритетная очередь; обрабатывается по
  одному элементу за 10 мс глобальным `setTimeout`-тикером, без бюджета по
  времени кадра, без отмены, без учёта направления движения.
- Кэш тайлов (`TSCache.storage`) не имеет верхнего предела и вытеснения —
  растёт неограниченно (`src/tile_tree.ts:94-95` фиксирует это как старый
  todo).
- Загрузка тайла (`makeTileGetter`, `src/layers.ts:3`) обрабатывает только
  `img.onload`; `onerror` не обработан — битый/404 тайл зависает в
  состоянии `'prepare'` навсегда и будет молча пере-запрашиваться.
- Источники тайлов — захардкоженные константы в `src/layers.ts`
  (`tsMerged`, `tsBack`, `tsFront`, `tsOSM`, `tsStrava`), без общего
  контракта схемы тайлов (Slippy XYZ vs TMS vs плоская xkcd-схема со
  сдвигом `z_max`) и без способа сконфигурировать источник извне.
- `Vector` (`src/vector.ts`) — только 2D вектор, нет матриц/афинных
  преобразований вообще; вращения и вложенные координатные системы на этом
  фундаменте не построить.
- В клиенте `src/` нет ни одного теста и нет тестового раннера
  (`package.json` содержит только `build`/`watch`/`typecheck`); есть только
  Python-тесты `world/`, к этому виджету не относящиеся.

---

## Фаза 1. Афинный стек преобразований (`AFF-*`)

Общий фундамент под вращение, "вьюпорты во вьюпортах" и точный zoom-to-cursor.

- [x] **AFF-1 — сделано.** Класс `Mat2D` в `src/mat2d.ts` (2×3 афинная
  матрица `[a b c d e f]`, как в Canvas2D `setTransform`). Методы:
  `multiply`, `invert`, `determinant`, `transformPoint`/`transformVector`
  (вектор — без переноса), `equals`, `clone`, `toString`; статические
  фабрики `identity/translation/scaling/rotation/fromDOMMatrix`. Покрыт
  14 юнит-тестами в `src/mat2d.test.ts` (vitest, `INFRA-1`), включая
  `invert(m).multiply(m) ≈ identity` в обе стороны, круговой
  `screenToWorld(worldToScreen(p)) ≈ p`, композицию `multiply` в обоих
  порядках на эталонных значениях, поворот на 90°/360°, и падение
  `invert()` на вырожденной матрице.

  **Важное архитектурное решение, принятое по ходу реализации:**
  `src/mat2d.ts` — единственный файл в `src/`, оформленный как настоящий
  ES-модуль (`export class Mat2D`), в отличие от всего остального кода
  (глобальные скрипты без `import`/`export`, склеенные через `<script>`
  теги в `docs/index.html` в фиксированном порядке — см. аудит выше).
  Иначе код было бы физически невозможно unit-тестировать через vitest.
  Он не ссылается на `Vector` как значение (только на ambient-тип `XY`
  — типы у TypeScript не требуют `import`, в отличие от значений),
  поэтому на момент написания этой задачи не тянул остальной код в
  модульную систему и не был подключён в `docs/index.html`.
  **Обновление из `AFF-3`:** этот момент наступил раньше, чем
  предполагалось здесь (не в `AFF-4`, а уже в `AFF-3`, как только
  `MapWidget` понадобился `camera: Transform2D`) — весь `src/`
  переведён на ES-модули, `XY`/`XArg` заодно перенесены из `vector.ts` в
  отдельный `src/types/geometry.d.ts` (иначе стали бы приватными для
  модуля `vector.ts`). Подробности и проверка через Playwright — в
  `AFF-3`.
- [x] **AFF-2 — сделано, с двумя уточнениями к формулировке.** Класс
  `Transform2D` в `src/transform2d.ts` (узел координатной системы):
  локальные `x/y/scaleX/scaleY/rotation` через get/set-аксессоры (bump
  версии только при реальном изменении значения), ссылка на
  `parent: Transform2D | null`, кэшируемые `localMatrix`/`worldMatrix`,
  `worldToLocal(p)`/`localToWorld(p)`.
  1. **Инвалидация — не версионный счётчик у родителя + сверка у
     потомка, а pull-based по идентичности объекта:** `worldMatrix`
     каждого узла — это стабильная ссылка на `Mat2D`, которая меняется
     только когда что-то реально пересчиталось; потомок просто сравнивает
     `parent.worldMatrix !== <ссылка, с которой в прошлый раз считал>`.
     Так инвалидация распространяется по цепочке сама, без обхода
     списка потомков у родителя — на практике проще версионных
     счётчиков и с тем же результатом (протестировано на 3 уровнях
     вложенности, включая инвалидацию у внука при изменении деда).
  2. **`screenToLocal`/`localToScreen` не реализованы в этой задаче** —
     они осмысленны только когда есть понятие "камеры" (`AFF-3`); делать
     их сейчас означало бы дважды проектировать один и тот же API.
     Осознанно отложено на `AFF-3`, не потеряно.
  Skew не реализован (в бэклоге и был помечен опциональным) — осталось
  вне API. 10 юнит-тестов в `src/transform2d.test.ts` (vitest):
  identity-узел, `translate`/`rotate`/`scale`-композиция на эталонном
  значении, кэш возвращает тот же объект пока ничего не менялось,
  инвалидация при реальном изменении и НЕ инвалидируется при
  присвоении того же значения, композиция родитель→потомок, инвалидация
  внука при изменении деда, репарентинг подхватывается на следующем
  чтении, и круговая проверка на 3 вложенных уровнях с поворотом и
  неравномерным масштабом на каждом (закрывает `AFF-5`, см. ниже).
  **Зависит от: AFF-1. Оценка: M.**
- [x] **AFF-3 — сделано, с двумя уточнениями к формулировке.**
  `MapWidget.camera: Transform2D` (`src/map.ts`): `x/y = c.x/c.y`,
  `scaleX/scaleY = 1/zoom_factor`, синхронизируется каждый кадр в
  `onRepaint`. Представляет мир ↔ "центрированное" пространство вьюпорта
  (начало координат — центр канваса, без поворота — тот придёт в
  `ROT-*`). `MapWidget.screenToWorld(p)`/`worldToScreen(p)` — тонкие
  обёртки поверх `camera.localToWorld`/`worldToLocal`, добавляющие сдвиг
  на `(canvas.width/2, canvas.height/2)` (реальные пиксели канваса,
  верхний левый угол — как `event.offsetX/offsetY`). Ручная арифметика
  в `dblclick`-хендлере (`src/map.ts`) заменена на `screenToWorld`; заодно
  найден и исправлен реальный баг — хендлер использовал `e.pageX/pageY`
  (координаты страницы) вместо канвас-относительных, что было верно
  только пока канвас стоит в начале координат страницы без скролла;
  теперь `e.offsetX/offsetY`, как и должно быть.
  1. **`devicePixelRatio` не учтён** — `ZOOM-8` всё ещё не сделан, и
     учитывать его в `camera` до этого преждевременно (нечего умножать).
     `camera`/`screenToWorld`/`worldToScreen` спроектированы так, чтобы
     `ZOOM-8` мог добавить множитель, не меняя архитектуру.
  2. **`screenToLocal`/`localToScreen` на `Transform2D` не добавлены** —
     по итогам реализации выяснилось, что это не общий метод узла:
     перевод "экранные пиксели → пространство камеры" зависит от живых
     `canvas.width/height`, которые знает только `MapWidget`, а не сам
     `Transform2D`-узел. Сделано наоборот: `MapWidget` держит этот сдвиг
     у себя (`screenToWorld`/`worldToScreen`), а `Transform2D` остаётся
     чистой абстракцией вложенных координат без знания об экране.
     Для `AFF-4`/`VP-*` понадобится похожий, но не идентичный метод на
     уровне слоя — решать тогда, не форсировать здесь абстракцию впрок.
     **Обновление из `AFF-4`:** предположение "слои станут дочерними
     узлами камеры" не подтвердилось — оказалось, что `camera` не
     годится в буквальные родители `Transform2D` слоя из-за инверсии
     `local`/`world` (см. `AFF-4`); слои остались независимыми корневыми
     узлами, композиция с камерой собирается явно.

  **Побочный, но необходимый эффект — миграция всего `src/` на настоящие
  ES-модули.** `AFF-3` буквально требовало, чтобы `map.ts` мог
  `import { Transform2D } from './transform2d.js'` как значение — а это
  невозможно, пока `map.ts` (и всё, что от него зависит и от чего
  зависит он) остаётся глобальным скриптом без `import`/`export`. Сделано
  полностью: `vector.ts`, `defines.ts`, `tools.ts`, `tile_tree.ts`,
  `tile_source.ts`, `map.ts`, `layers.ts`, `index.ts`, `test_heating.ts`
  — все получили явные `export`/`import` (со специфичной для браузерных
  ES-модулей деталью: относительные импорты пишутся с явным `.js` —
  `import { Vector } from './vector.js'` — TypeScript резолвит это в
  `vector.ts` для тайпчека, а в выводе строка остаётся буквально `.js`,
  что и нужно браузеру; `tsc` **не** ловит забытое расширение, если оно
  просто отсутствует — это на совести автора правки, проверено `grep`).
  `docs/index.html`/`docs/test_heating.html` переведены на один
  `<script type="module" src="js/index.js">` (`test_heating.js`
  аналогично) вместо восьми вручную упорядоченных `<script>`-тегов —
  граф зависимостей теперь строит сам браузер по `import`. Классические
  `<script>`-теги (`crossbrowser_patch.js`, `dat.gui.min.js`,
  `full_xkcd_ts_data.js` — полифилл, вендорный UMD и сгенерированный
  data-блоб) сознательно оставлены как есть: они успевают выполниться до
  того, как стартует любой отложенный `type="module"`-скрипт, так что
  `TILES_AS_TREE`/`dat` остаются доступны как ambient-глобали безо
  всякого импорта (см. `src/types/tiles-data.d.ts`/`dat-gui.d.ts`).
  Заодно `XY`/`XArg` вынесены из `vector.ts` в отдельный
  `src/types/geometry.d.ts` (иначе они стали бы приватными для модуля
  `vector.ts`, а не общими ambient-типами, как рассчитывали `mat2d.ts`/
  `transform2d.ts` в `AFF-1`/`AFF-2`), а мёртвая `let layer_background`
  в `index.ts` удалена (уже требовалось трогать этот файл целиком).

  **Проверено сквозным браузерным тестом через Playwright** (см. ниже,
  "Playwright настроен и проверен"): собранная `docs/`-сборка отдана
  через локальный HTTP-сервер, страница открыта в headless Chromium,
  граф из 10 ES-модулей (`index.js` → `map.js`/`layers.js` → …)
  подгрузился браузером сам по себе без единой ошибки загрузки модулей;
  синтетический `dblclick` по канвасу с оффсетом `(200, 100)` от центра
  при `zoom_factor=1` дал точный ожидаемый URL
  `#[40372636,22578835]` (= `40373076-440, 22579095-260`) — то есть
  `screenToWorld` считает абсолютно точно.
- [x] **AFF-4 — сделано, с важной поправкой к формулировке.** Каждый
  `Layer` получил `transform: Transform2D` (`this.shift`/новые опции
  `scale`/`rotation` читаются в него один раз при конструировании).
  `TiledLayer.draw`/`getLevelParams` переписаны: LOD-подбор (какой
  целочисленный `z`/`k`/`tile_size` использовать) остался прежним — это
  дискретный выбор "что тащить", а не то, что решается матрицей; а вот
  ПОЗИЦИОНИРОВАНИЕ тайлов теперь идёт через один composed `Mat2D` на
  весь слой за кадр (`computeTileGridMatrix`) и реальный
  `ctx.setTransform(...)` + `drawImage` в единичный квадрат
  (`(ix, iy, 1, 1)` в локальных тайл-индексных координатах) вместо
  ручного `x = ix*tile_size - c.x + w/2`.

  1. **"Дочерний по отношению к камере" — не буквально.** Выяснилось
     при реализации: у `camera` направления `local`/`world` намеренно
     инвертированы (`local`=центрированный экран, `world`=общие мировые
     координаты — чтобы `screenToWorld`/`worldToScreen` читались
     естественно на уровне `MapWidget`). Если буквально сделать
     `layer.transform.parent = camera`, локальный выход слоя попадёт во
     ВХОД камеры (её `local`, то есть экранное пространство), а не в её
     `world` — направление противоположно нужному. Поэтому
     `layer.transform` — независимый корневой узел (`parent: null`),
     который сам отображает "локальные координаты слоя" прямо в общее
     мировое (`position`) пространство; `map.camera` берёт уже готовую
     мировую точку оттуда, как и любую другую. Композиция собирается
     явно в `computeTileGridMatrix`, а не через встроенный
     parent-chaining `Transform2D` — потому что для этой пары узлов
     (`camera`+`layer.transform`) он "не той стороной".
  2. **Контракт `onTileDraw` не меняется.** Полный перевод и
     debug-оверлеев (`drawTileDebug`, сетка `map_grid` — оба используют
     `ctx.font`/`ctx.lineWidth` в CSS-пикселях) на локальные тайл-индексные
     единицы потребовал бы компенсировать масштаб самим этим коллбэкам
     (`ctx.font`/`lineWidth` тоже подчиняются активной матрице
     трансформации) — ради rotation-осведомлённости, которая debug-слоям
     не нужна. Поэтому `ctx.setTransform` используется только для
     отрисовки самого изображения тайла (`tileDraw`, per-tile
     `save`/`setTransform`/`drawImage`/`restore`); `onTileDraw`
     по-прежнему получает готовые пиксельные `x`/`y`/`tsize`,
     посчитанные той же матрицей заранее — код в `layers.ts` не тронут.
  3. **Выбор диапазона видимых тайлов (`tx`/`ty`/`dx`/`dy`) всё ещё
     считается в предположении identity-трансформации слоя** — то есть
     при нестандартном `shift`/`scale` возможна неоптимальная выборка
     (пропуск тайла у края или лишняя загрузка), хотя ПОЗИЦИЯ уже
     отрисованных тайлов всегда верна. Честный учёт shift/scale здесь
     означал бы проекцию четырёх углов канваса через обратную матрицу
     слоя — ту же технику, что `ROT-3` всё равно потребует для
     повёрнутого вьюпорта; делать дважды не имеет смысла, довести до ума
     тогда, когда до этого дойдёт первым — `AFF-4` или `ROT-3`.

  **Проверено на двух уровнях:**
  - `src/map.test.ts` — 9 тестов, DOM-независимых: новая
    `computeTileGridMatrix` (публичная, чистая функция) сверена с
    независимо переписанной СТАРОЙ формулой `x*tile_size-c.x+w/2` на 6
    комбинациях позиции/зума/размера тайла, включая зум ровно на границе
    степени двойки — совпадение с точностью до 1e-6 на identity-трансформации
    слоя (это ЛЮБОЙ существующий сегодня слой). Плюс отдельные тесты на
    то, что ненулевой `shift`/`scale` слоя сдвигает/масштабирует тайлы
    ровно на ожидаемую величину.
  - Playwright, сквозная проверка живого рендера: скриншот в
    дефолтном состоянии (`pos=(40373076,22579095)`) — пиксель-в-пиксель
    совпадает с самым первым скриншотом этой сессии (до всех правок);
    затем синтетический `wheel` (зум-аут с якорем в стороне от центра) —
    позиция и масштаб сместились ожидаемо, сетка по-прежнему точно
    совпадает со зданиями/дорогами на новом уровне `z`/`tile_size`, без
    швов и артефактов.
  **Зависит от: AFF-2, AFF-3. Оценка: L** (не выше исходной — риск
  подтвердился, но был закрыт до реализации выбором безопасного
  scope: LOD-подбор не трогать, `onTileDraw` не трогать, точный
  diff-инвариант "identity-трансформация ⇒ побитово старая формула" —
  проверяемый юнит-тестом, а не только на глаз).
- [x] **AFF-5 — сделано попутно с `AFF-2`, с уточнением формулировки.**
  Тесты на композицию 2-3 уровней вложенных `Transform2D` с поворотом и
  неравномерным масштабом на каждом уровне уже написаны в
  `src/transform2d.test.ts` (см. `AFF-2`). **Уточнение:** буквально
  `screenToWorld(worldToScreen(...))`, как было сформулировано изначально,
  на этом этапе проверить нельзя — этих методов ещё нет, они появятся
  только в `AFF-3` вместе с камерой. Проверена содержательно та же
  мысль — круговой проход `worldToLocal(localToWorld(p)) ≈ p` через
  цепочку вложенных `Transform2D` (что и является сутью задачи: что
  композиция трансформаций через несколько уровней работает корректно и
  обратимо). Буквальную `screenToWorld`/`worldToScreen`-версию имеет
  смысл добавить как маленький довесок в `AFF-3`, когда появится камера
  — не как отдельная задача. **Обновление:** литеральный
  `screenToWorld`/`worldToScreen` round-trip теперь тоже покрыт — но не
  юнит-тестом, а сквозной Playwright-проверкой в рамках `AFF-3` (см.
  выше), поскольку он требует реального канваса/DOM.
- [ ] **AFF-6 (отложено, вне текущего бэклога)** — Лёгкий перспективный
  наклон плоскости (2.5D tilt). Решено пока не браться: самый
  спекулятивный пункт исходной заявки. Если/когда вернёмся к нему —
  предпочтительный путь обсуждён и зафиксирован: честная перспективная
  математика на Canvas2D, а не параллельная система через CSS3D
  `matrix3d`. **Уточнение по итогам `ROT-3`:** предполагавшийся "тот же
  офф-скрин буфер, что и ROT-3" не появился — вращение обошлось без
  буфера (см. `ROT-3`). Наклон, если до него дойдёт, скорее всего
  ляжет на `computeTileGridMatrix` похожим образом (перспективное
  слагаемое в композиции матриц), но это отдельная, ещё не
  спроектированная математика (полноценная перспектива требует
  проекционного деления, `Mat2D` — чисто аффинный, без него). Не
  заводить подзадачи и не резервировать под это работу других фаз — они
  рассчитаны так, чтобы AFF-6 не блокировал ничего остального. **Оценка
  при возврате: XL.**

## Фаза 2. Плавный и комфортный зум (`ZOOM-*`)

- [x] **ZOOM-1 — сделано, с уточнением к формулировке.** Zoom-to-cursor
  реализован не как разовая коррекция "до/после", а как **коррекция на
  каждом кадре** анимации зума: `wheel`-хендлер запоминает точку курсора
  в `_zoom_anchor_screen`, а `onRepaint` на каждом шаге сглаживания
  `zoom_factor` сдвигает `this.c` на `centered * (1/old_zoom - 1/new_zoom)`
  (выведено напрямую из формулы `screenToWorld`) — так курсорная точка не
  "плывёт" в процессе анимации, а держится на месте непрерывно, а не
  только в момент, когда зум долетает до цели. Клавиатурный зум (`+`/`-`)
  сбрасывает якорь в `null` перед вызовом `zoomIn`/`zoomOut` — курсору
  неоткуда взяться, зумит вокруг центра, как раньше.
  **Проверено сквозным Playwright-тестом:** синтетический `wheel` в точке
  со смещением `(-440,-260)` от центра канваса, зум `1 → 0.8`, ожидание
  полного оседания анимации — итоговая позиция `(40373186, 22579160)`
  совпала до пикселя с независимо посчитанной вручную (по сырой формуле
  `world = centered/zoom + c`, не по формуле коррекции из кода — то есть
  проверка не циклична). **Зависит от: AFF-3. Оценка: S** (оказалось
  проще, чем ожидалось M — `screenToWorld`/`camera` из `AFF-3` дали всю
  нужную математику готовой).
- [x] **ZOOM-2 (Фаза 0) — сделано частично.** Анимация зума переведена на
  `dt`-based экспоненциальное сглаживание (`src/map.ts`, `onRepaint`),
  откалибровано так, чтобы при номинальных 60fps ощущалось так же, как
  старая формула; `dt` клампится на первом кадре (`NaN`) и на длинных
  паузах (свёрнутая вкладка), чтобы не было скачка. **Панорамирование с
  инерцией (`inertion_value`/`sliding_value`, `scrollType`
  `inertial`/`sliding`) намеренно не тронуто в этом проходе** — тот же
  fix для него требует пересчёта констант трения в секундах, а не в
  кадрах, это отдельное визуальное поведение, которое стоит проверять
  интерактивно перед мержем. Осталось как отдельный подпункт: перевести
  `_scroll_velocity`-затухание на dt так же, как сделано для зума.
  **Обновление — найден и исправлен баг в самом счётчике FPS в
  отладочной информации** (по прямому запросу пользователя: "почини
  отображение FPS"). Причина в двух слоях: (1) `onRepaint` считал `dt`
  через `new Date().getTime()` — миллисекундное разрешение; на 120Hz+
  дисплее (или просто когда два кадра рендерятся в один и тот же
  миллисекундный тик — рутина, не редкий случай) это давало `dt=0`, а
  `fps = Math.round(1/dt) = Infinity`; (2) `AvgRing.add()` (`src/tools.ts`)
  отсеивал только "falsy" значения (`!value`) — `Infinity` не falsy,
  проходил внутрь и **необратимо** портил `sum`/`last_sum`
  (`Infinity + x = Infinity` навсегда, а после вытеснения этого сэмпла
  из кольца `Infinity - Infinity = NaN`, дальше отравляя каждое
  следующее среднее). Исправлено на обоих уровнях: `onRepaint` теперь
  берёт время через `performance.now()` (субмиллисекундное разрешение —
  устраняет саму причину `dt=0`), `AvgRing.add()` вдобавок отбрасывает
  любое нефинитное значение (`!isFinite(value)`) как защиту "на всякий
  случай", независимо от того, что именно её питает.
- [x] **ZOOM-3 — сделано, но не через Pointer Events (решение
  пересмотрено).** По прямому отчёту пользователя: "добавь поддержку
  тачскрина, сейчас вообще не работает ни скроллинг, ни масштабирование,
  ни поворот" — на сенсорном экране `mouse*`-события либо не приходят
  вовсе, либо приходят с задержкой/не для каждого касания, а `wheel`
  не приходит вообще, так что ни одна из существующих панорамирование/
  зум/поворот-привязок не работала.

  Изначальный план предполагал единый `pointerdown/pointermove/
  pointerup/pointercancel` слой ВМЕСТО `mouse*`, что попутно дало бы и
  тач. На практике это означало бы переписывать уже отлаженные
  mouse-обработчики (drag-панорамирование, Shift+drag-поворот) ради
  унификации, которая сама по себе не была нужна — проще и с меньшим
  диффом оказалось добавить отдельные `touch*`-обработчики
  (`touchstart`/`touchmove`/`touchend`/`touchcancel`) РЯДОМ с
  существующими `mouse*`, переиспользующие ту же внутреннюю
  инфраструктуру (`_dx`/`_dy`, `zoomBy()`+`_zoom_anchor_screen`,
  `_drotation`) вместо параллельной реализации. Названо `ZOOM-3` (тот
  же тикет), а не заведено заново — Pointer Events сами по себе как
  унификация остаются на будущее (заведено ниже как
  **ZOOM-3-pointer-events**), но не блокируют это решение.

  Один палец — панорамирование (тот же путь, что и mouse-drag, включая
  инерцию: `touchstart`/`touchmove`/`touchend` выставляют
  `_mouse_move_flag`/`_mouse_down_flag` так же, как `mousedown`/
  `mouseup`, так что `scrollType: 'sliding'` даёт touch-панорамированию
  тот же накат по инерции, что и мыши, бесплатно). Два пальца —
  панорамирование (по центроиду) + pinch-zoom (по отношению расстояний
  между кадрами, с анкором в середине между пальцами — тот же приём,
  что и у `wheel`) + поворот (по изменению угла между пальцами,
  напрямую в `_drotation` — как и Shift+drag) ОДНОВРЕМЕННО, стандартный
  для мобильных карт набор жестов (Google/Apple/Leaflet делают так же).
  Все три считаются как ДЕЛЬТА между последовательными `touchmove` (тот
  же стиль, что и `old_x`/`old_y` у мыши), а не от фиксированной точки
  начала жеста — устойчиво к добавлению/снятию пальца посреди жеста
  (снимок состояния пересчитывается заново на каждом `touchstart`/
  `touchend`, не только на `touchmove`). Угол между пальцами через
  `atan2` оборачивается на ±π — наивное вычитание давало бы скачок на
  ~2π в момент пересечения этой границы; нормализовано через
  `atan2(sin(Δ), cos(Δ))`.

  `canvas.style.touchAction = 'none'` — иначе браузер применяет
  собственную обработку жестов (пинч-зум страницы, скролл тачем)
  параллельно с нашей, конкурируя за те же касания.

  **Проверено через Playwright** (синтетические `Touch`/`TouchEvent`,
  диспетчеризуемые напрямую на канвас): один палец, сдвиг на 80px при
  `zoom_factor=0.5` — даёт мировую дельту 160 (80/0.5), как и ожидается;
  `_mouse_move_flag` корректно сбрасывается на `touchend`. Двумя
  пальцами: раздвижение с 100px до 200px даёт `zoom_target` `0.5→1.0`
  (клампится в `zoom_max`), сближение с 200px до 100px — `0.5→0.25`;
  поворот пальцев на 90° даёт `rotation` ровно 90°. Три пальца и
  внезапный `touchcancel` не роняют обработчик (третье касание просто
  не участвует в pinch/поворот-геометрии, только в центроиде для
  панорамирования).

  **Follow-up (по прямому отчёту пользователя: "мультитач вращение
  крутит карту не в ту сторону").** Дельта угла между пальцами
  (`current.angle - previous.angle`, в экранных пиксельных координатах,
  y вниз) прибавлялась к `_drotation` напрямую. Проверено через
  прямое построение `gridMatrix` при разных `camera.rotation`
  (`gm.b`/`gm.c` при `rotation=0.1` совпали с `Mat2D.rotation(-0.1)`, не
  `+0.1`): `rotation` — это поворот КАМЕРЫ относительно мира, а
  `camera.worldMatrix.invert()` (то, что реально ставит контент на
  экран) применяет угол со знаком МИНУС — рост `rotation` поворачивает
  видимый контент ПРОТИВ часовой стрелки, не по часовой. Складывая
  сырую дельту угла пальцев напрямую, код поворачивал карту в сторону,
  обратную жесту. Исправлено вычитанием (`_drotation -= ...`) вместо
  прибавления. Заодно поймана и исправлена **неверная документация**:
  докстринг `rotateBy()` утверждал "positive = clockwise on screen,
  matching Mat2D.rotation" — неверно по той же причине; docstring
  исправлен на точное описание, поведение `[`/`]` и Shift+drag НЕ
  тронуто (пользователь не жаловался на них, это был только баг в новой
  тач-логике). **Проверено:** поворот пальцев по часовой стрелке (правый
  палец от 3 к 5 часам, левый от 9 к 11) теперь даёт `rotation = -45°`
  — что через ту же формулу соответствует контенту, повёрнутому по
  часовой стрелке на экране (визуально подтверждено скриншотом:
  проспект, шедший горизонтально, лёг по диагонали в сторону жеста).
  - **ZOOM-3-pointer-events** — Унификация `mouse*`+`touch*` в единый
    `pointerdown/pointermove/pointerup/pointercancel` слой (заодно
    закрывает поддержку stylus/pen) — чисто рефакторинг, никакого
    нового поведения; не взято сейчас ради меньшего диффа и риска
    сломать уже отлаженные mouse-обработчики. **Оценка: M, приоритет
    низкий.**
- [x] **ZOOM-4 (Фаза 0) — сделано.** `e.keyCode` заменён на проверку
  `e.key`/`e.code` против конфигурируемых `MapWidgetOptions.zoomInKeys`/
  `zoomOutKeys` (дефолты покрывают основной ряд клавиш и нампад). Заодно
  логика зума из `wheel`- и `keydown`-обработчиков (была продублирована)
  вынесена в `MapWidget.zoomIn()`/`zoomOut()`.
  **Обновление — непрерывный зум на удержании `Z`/`X`** (по запросу
  пользователя: "не понятно как крутить карту, добавь биндинг на z/x").
  В отличие от `zoomInKeys`/`zoomOutKeys` (разовый шаг на `keydown`),
  `zoomInHoldKeys`/`zoomOutHoldKeys` (дефолты `['x']`/`['z']`) держатся в
  том же `Set<string>` живых нажатий, что и WASD (см. `ROT-1`), и на
  каждом кадре `onRepaint` зовут тот же `zoomBy()`, что колесо/`+`/`-` —
  бесплатно наследует его мягкое торможение у `zoom_min`/`zoom_max`
  (`ZOOM-5`). Множитель за кадр — `KEYBOARD_ZOOM_RATE_PER_SEC^frame_dt`
  (степень, не произведение) так, чтобы скорость зума в "разах в
  секунду" не зависела от fps. Курсорного анкора нет (как и у `+`/`-`) —
  зумит вокруг центра, `_zoom_anchor_screen` сбрасывается в `null` перед
  вызовом. **Проверено через Playwright:** держал `z` 800мс на
  `zoom_target=1` (максимум) → `0.6` (ожидалось `~0.57`, за вычетом
  раздутия от накладных расходов тестового рантайма на реальное время
  удержания); следом `x` 800мс от `0.6` → упёрся обратно в `1` (мягкое
  торможение о верхнюю границу сработало).
- [x] **ZOOM-5 — сделано, выбран более лёгкий из двух вариантов.** Взят
  вариант "нелинейное затухание", не полноценный overshoot+пружина:
  `MapWidget.zoomBy()` (общий для `zoomIn`/`zoomOut`) при шаге,
  пересекающем границу, продвигает `zoom_target` только на половину
  оставшегося расстояния до неё (`ZOOM_EDGE_SOFTNESS = 0.5`), а не
  жёстко клампит и не отказывает в движении вовсе — так подход к границе
  ощущается как плавное торможение, а не стена. Шаг в обратную сторону
  (от границы) всегда полный, без затухания. Overshoot+spring-back не
  взят сознательно: он требует таймера "ещё ли активен жест
  пользователя, чтобы начать возврат" — лишняя стейт-машина ради
  эффекта, который эта более простая версия даёт почти так же хорошо.
  Не проверялось через Playwright, в отличие от `ZOOM-1`/`AFF-3` — здесь
  чистая арифметика без канваса/координат/тайминга, логика прослежена
  вручную по шагам и совпадает с кодом; риск ниже, чем у сквозных
  DOM-зависимых правок. **Зависит от: ZOOM-2. Оценка: S.**
- [ ] **ZOOM-6** — Настраиваемая кривая easing для анимации зума/пана
  (linear/quad/cubic/spring), вынести в `MapWidgetOptions.zoomEasing`.
  **Зависит от: ZOOM-2. Оценка: S.**
- [x] **ZOOM-7 (Фаза 0, исправление бага) — сделано.** `canvas2`/`ctx2`
  удалены — код нигде больше не использовался, сохранять было нечего.
  **Обновление:** предполагавшийся под это офф-скрин буфер для `ROT-3`
  так и не понадобился — вращение решилось без буфера (см. `ROT-3`).
- [ ] **ZOOM-8** — Учитывать `devicePixelRatio` в `onResize` и отрисовке
  (масштабировать канвас в физических пикселях, CSS-размер — в
  логических), чтобы на HiDPI-экранах не было размытия. **Оценка: S-M.**
- [x] **ZOOM-9 — сделано.** Найдена и исправлена причина **быстрого
  дрожания картинки в момент остановки масштабирования** — по прямому
  запросу пользователя ("вижу дрожание в момент остановки масштабирования
  на максимальном увеличении"). Причина была не в анимации/математике
  зума (`zoom_factor`/`c` — проверено логированием их значений по кадрам
  через синтетический wheel-жест до полного оседания: сходятся гладко,
  монотонно, без единого разворота знака), а в обработчике `wheel`
  (`src/map.ts`): он применял **один и тот же фиксированный шаг**
  (`zoomIn()`/`zoomOut()`, `zoom_step_factor` = 20% по умолчанию) на
  КАЖДОЕ событие `wheel`, вообще не учитывая величину `deltaY`. Обычное
  колесо мыши шлёт одно событие на "щелчок" — с этим полный
  фиксированный шаг ощущается нормально. Но тачпад шлёт плотный поток
  событий на один жест, и его инерционный "хвост" (после того, как
  пальцы уже физически остановились) обычно вырождается в несколько
  крошечных, **знакопеременных** событий (например `-3, +2, -1, +1`) —
  каждое из них раньше вызывало ПОЛНЫЙ шаг зума в противоположную от
  предыдущего сторону, что на глаз выглядит ровно как быстрое дрожание
  картинки, ровно в тот момент, когда скролл визуально "остановился".
  Воспроизведено синтетически (`wheel` с чередующимися маленькими
  `deltaY`) — `zoom_target` реально скакал `1.0 → 0.8 → 0.96 → 0.77 → …`
  на одних лишь шумовых событиях величиной 1-3. Исправлено: шаг теперь
  масштabируется по `|deltaY|` — `Math.pow(1 + zoom_step_factor,
  deltaY / WHEEL_DELTA_PER_STEP)`, где `WHEEL_DELTA_PER_STEP = 100`
  откалиброван так, что один "щелчок" физического колеса мыши
  (`|deltaY| ≈ 100` во всех проверенных браузерах/ОС) даёт ТОЧНО тот же
  шаг, что и раньше (никакого регресса для мыши), а шумовые
  тачпад-события величиной 1-3 дают едва заметное (доли процента)
  изменение цели вместо полного шага. **Проверено тем же синтетическим
  тестом:** тот же самый шумовой хвост (`-80,-60,-40,-20,-8,-3,2,-1,1,
  -1,3,-2`), который раньше давал `zoom_target` в диапазоне `0.73…1.0` с
  несколькими разворотами, теперь даёт монотонное схождение `1.0 → …→
  0.9964` без единого разворота знака; одиночное событие `deltaY=-100`
  (полный щелчок мыши) по-прежнему даёт тот же шаг, что и до правки
  (с поправкой на `ZOOM-5`-смягчение у границы `zoom_max`, которое
  применяется поверх и не тронуто). **Зависит от: ZOOM-5. Оценка: S**
  (после того, как настоящая причина нашлась — искать её пришлось не
  осмотром кода, а логированием `c`/`zoom_factor`/`zoom_target` по
  кадрам через временный monkey-patch `MapWidget.prototype.onRepaint`,
  поскольку сама eased-анимация зума оказалась ни при чём).

  **Follow-up (по прямому отчёту пользователя после первой правки:
  "дрожание усилилось, теперь вообще при любом изменении зума") —
  найдена и исправлена вторая, более серьёзная проблема в том же
  месте.** Масштабирование шага по `|deltaY|` без верхнего предела было
  ошибкой в другую сторону: реальный тачпад в АКТИВНОЙ (не хвостовой)
  части жеста рутинно репортит `deltaY` в разы больше калибровочных
  ~100 (в проверке — всплески до 300), и без потолка формула
  `Math.pow(1 + zoom_step_factor, deltaY / 100)` превращала одно такое
  событие в многократный (несколько сотен процентов) скачок цели зума —
  куда более заметное и частое дрожание, чем старый фиксированный шаг
  20% когда-либо давал, и уже не только на "хвосте", а на любом
  событии с завышенной величиной. Исправлено добавлением потолка:
  `|deltaY|` сначала обрезается до `WHEEL_DELTA_PER_STEP` (100), и уже
  обрезанное значение идёт в ту же формулу — маленькие шумовые события
  по-прежнему масштабируются пропорционально (это чинило исходный
  баг), но одно событие больше никогда не может дать шаг сильнее
  старого фиксированного `zoomIn()`/`zoomOut()`. **Проверено через
  Playwright:** синтетический "быстрый свайп" (`-300,-260,…,-100`,
  затем шумовой хвост `-8,-3,2,-1,1,-1,3,-2`) — `zoom_target` больше не
  улетает за пределы мягкого клампа у границы, `zoom_factor` (то, что
  реально рисуется) сходится к финальному значению МОНОТОННО, без
  единого разворота, за весь жест; одиночное `deltaY=±100` по-прежнему
  даёт точно `×1.2`/`×(1/1.2)` — регресса для обычной мыши нет.

- [x] **ZOOM-10 — сделано.** По прямому отчёту пользователя, после
  `ZOOM-9`: "дрожание так никуда и не делось, стало как было до первой
  правки... дрожат только тайлы под сеткой, сетка не дрожит... видно и
  при масштабировании с клавиатуры" — то есть `ZOOM-9` чинил реальный,
  но НЕ основной источник дрожания: настоящая причина не в источнике
  ввода (колесо/клавиатура) вообще, а в отрисовке тайлов. Диагноз:
  `TiledLayer.tileDraw()` вызывал `ctx.setTransform(gridMatrix...)`
  с координатами матрицы КАК ЕСТЬ, а рисовал по индексу тайла (`ix,
  iy`) — собственный `e/f` (сдвиг) у `gridMatrix` включает в себя
  позицию камеры в мировых координатах, а это порядка 10⁷ на масштабе
  этого мира. Растеризатор canvas хранит текущую матрицу трансформации
  в одинарной точности (`float32`) — на величине ~4.5×10⁷ это даёт
  разрешение всего в несколько **экранных пикселей**. Незаметно, пока
  сдвиг стоит на месте, но при ЛЮБОМ непрерывном изменении (доводка
  зума, вращение, удержание Z/X) истинное (double-precision) значение
  смещается на доли пикселя каждый кадр, и то, в какую сторону это
  округлится в `float32`, от кадра к кадру не совпадает — тайл видимо
  подрагивает на несколько пикселей. Хуже на максимальном зуме (больше
  индекс тайла → больше сдвиг → больше абсолютная ошибка округления) и
  одинаково от ЛЮБОГО источника изменения — ровно то, что описал
  пользователь. У сетки и debug-оверлея (`ROT-4`) этой проблемы никогда
  не было: они считают каждую точку в JS через `gridMatrix.
  transformPoint` (double precision) и передают растеризатору уже
  маленькие экранные координаты напрямую, никогда не запекая большое
  число в саму CTM.

  **Исправление:** та же линейная часть матрицы (`a/b/c/d` — её
  величина всегда умеренная, далеко от предела точности `float32`), но
  вместо собственного `e/f` матрицы в `ctx.setTransform` подставляется
  уже вычисленный экранный левый верхний угол тайла (`x`/`y` — обычная
  экранная величина), а рисование идёт от локального (0, 0) вместо
  (ix, iy). Математически точно то же самое размещение (проверено:
  локальная (0,0) даёт ровно (x, y), как и `gridMatrix.
  transformPoint({x: ix, y: iy})`) — меняются только числа, которые
  несёт сама CTM.

  **Не воспроизведено попиксельно в песочнице:** headless Chromium
  здесь рендерит через SwiftShader (программный рендерер — подтверждено
  через `WEBGL_debug_renderer_info`), что, вероятно, не показывает
  именно тот GPU-специфичный путь через `float32`, который есть у
  настоящего аппаратно ускоренного браузера (ровно там, где баг и был
  обнаружен пользователем). Вместо этого проверено, что фикс
  действительно меняет то, что доходит до canvas: перехвачены все
  вызовы `ctx.setTransform` при отрисовке тайлов на максимальном зуме
  до и после — сдвиг упал с ~10⁷ (например `-44513268`) до обычной
  экранной величины (например `-468`); и что новое размещение
  алгебраически точно эквивалентно старому. **Требует подтверждения на
  реальном железе** (у пользователя, где баг и наблюдался).

- [x] **ZOOM-11 — сделано.** По прямому запросу пользователя (в
  контексте `ZOOM-3`/тача, но баг был общим для `scrollType: 'sliding'`
  — дефолт, задан в `index.ts`): "нужно отключать инерцию при удержании
  пальцев на экране. карта должна следовать за пальцами в точности...
  инерция должна сохраняться только при отпускании". `'sliding'`
  накапливал затухающую `_scroll_velocity` (`.add(...)`) КАЖДЫЙ кадр,
  пока палец/кнопка мыши удерживались, и добавлял её ПОВЕРХ уже точного
  `this.scroll(this._dx, this._dy)` — то есть даже при удержании
  движение было не 1:1 с жестом, а "точное движение плюс нарастающий
  шлейф". Для сравнения, `'inertial'` (соседний режим) уже делал именно
  то, что нужно: `.set(...)` (не `.add()`) во время движения — просто
  запоминает "кандидата" скорости на случай отпускания прямо сейчас, и
  применяет/затухает её ТОЛЬКО когда контакт уже разорван (`else`-ветка
  на `_mouse_move_flag`).

  Исправлено переносом `'sliding'` на ту же структуру: во время
  контакта — точное `scroll()` плюс перезапись (не накопление)
  `_scroll_velocity` текущим кадровым движением (с тем же
  `sliding_value`-масштабированием, что и раньше — только `.set()`
  вместо `.add()`); после разрыва контакта — то, что успело
  накопиться в `_scroll_velocity` на момент отпускания, продолжает
  двигаться и плавно затухает (`div(inertion_value+1)` каждый кадр, как
  и раньше). **Проверено через Playwright** (синтетические тач-жесты,
  синхронизированные с реальными кадрами через
  `requestAnimationFrame`): равномерное движение на 5px/кадр даёт РОВНО
  линейный прирост позиции по кадрам (-5,-10,...,-50 — без каких-либо
  сверхлинейных добавок); удержание пальца неподвижным 5 кадров подряд
  не даёт дрейфа; отпускание после неподвижного удержания не даёт
  доп. движения; отпускание СРАЗУ после быстрого движения (10px/кадр)
  даёт плавно затухающее продолжение движения — первый шаг после
  отпускания (`-1.5`) в точности равен `10 * sliding_value` (10*0.15).

- [x] **ZOOM-12 — сделано.** По прямому отчёту пользователя: "мультитач на
  макбуке... работает отвратительно: плохо масштабирует, плохо поворачивает
  двумя пальцами, нет инерции вообще". Корневая причина: тачпад ноутбука в
  браузере — это **не** источник `TouchEvent` вообще (`ZOOM-3`'s
  `touchstart`/`touchmove` их никогда не получает от тачпада — только от
  настоящего сенсорного экрана телефона/планшета, откуда и разница "на
  смартфоне всё хорошо"); все жесты тачпада браузер синтезирует в
  `wheel`-события (и, только в Safari, отдельные `gesturestart`/
  `gesturechange`/`gestureend`). До этой правки обработчик `wheel`
  (`src/map.ts`) применял **любое** его событие как зум (`zoomBy`,
  `ZOOM-9`/`ZOOM-10`) — то есть обычный свайп двумя пальцами (задуман как
  панорамирование) тоже зумил, `deltaX` не читался вообще, ни `ctrlKey`, ни
  какая-либо эвристика типа устройства не использовались. Отсюда все три
  жалобы разом.

  **Реализовано без изобретения заново, портированием проверенных решений:**
  - **Зум vs панорамирование по `wheel`:** `e.ctrlKey || e.metaKey` —
    браузерный (Chrome с M35, Firefox с 55) стандартный сигнал именно
    pinch-жеста тачпада (а также физического Ctrl+колесо) — при нём идёт
    существующий путь `ZOOM-9`/`ZOOM-10` (`_wheelZoom`), совершенно без
    изменений, классификатор ниже даже не вызывается.
  - **Мышь vs тачпад, когда `ctrlKey` ложный** — портирована почти
    дословно боевая эвристика `mapbox-gl-js` (`src/ui/handler/
    scroll_zoom.ts`, MIT): нормализация `deltaMode === DOM_DELTA_LINE ?
    deltaY*40 : deltaY`; `value % 4.000244140625 === 0` → точно колесо
    мыши; `Math.abs(value) < 4` → точно тачпад; иначе, если пауза с
    прошлого события `> 400`мс — новый жест, тип пока неизвестен (запускается
    `setTimeout` на 40мс, который по истечении, если ничего так и не
    разрешило классификацию, считает жест одиночным щелчком мыши и
    фиксирует тип `'wheel'`); иначе — `Math.abs(timeDelta * value) < 200` →
    тачпад, else колесо. Вынесено в отдельную чистую функцию
    `classifyWheelEvent` (состояние — `WheelClassifyState` — передаётся
    явным параметром, а не хранится в замыкании) именно ради
    юнит-тестируемости без браузера/DOM — `MapWidget` держит его в
    `_wheel_state`/`_wheel_last_time`/`_wheel_grace_timer`. Классифицированное
    `'wheel'` (и неопределившееся `null`, ещё внутри grace-окна) идёт в
    зум (`_wheelZoom` — старое поведение мыши, без регресса); `'trackpad'`
    — в новый `_wheelPan`.
  - **Панорамирование по тачпад-свайпу, без своей инерции.** `_wheelPan`
    читает и `deltaX`, и `deltaY` (раньше `deltaX` не читался вовсе),
    делит на `zoom_factor` (та же конвенция, что у `_dx`/`_dy` везде в
    файле) и идёт через уже rotation-aware `scroll()` (`ROT-6`) —
    напрямую, БЕЗ накопления собственной `_scroll_velocity`: macOS/Windows
    Precision Touchpad сама шлёт после отрыва пальцев естественно
    затухающий поток `wheel`-событий (нативный momentum scroll,
    `preventDefault()` его не останавливает) — применение каждого
    классифицированного как `'trackpad'` события как точного `scroll()`
    (тот же принцип "точно во время контакта", что и `ZOOM-11`) даёт
    швырок-и-накат сам по себе, без дублирования физики.
  - **Поворот двумя пальцами по тачпаду — только Safari, платформенное
    ограничение, не полурешение.** `gesturestart`/`gesturechange`/
    `gestureend` навешаны на канвас через feature-detection
    (`'ongesturestart' in window`), без UA-снифинга — в Chrome/Firefox эти
    события не существуют и не всплывают, там код просто ничего не
    навешивает. Дельта `e.rotation` (кумулятивные градусы с
    `gesturestart`, не дельта на событие — дельта берётся между
    последовательными `gesturechange`) переводится в радианы и уходит
    прямо в `_drotation` — тот же аккумулятор, что и у Shift+drag/
    тач-поворота (`ROT-1`/`ZOOM-3`), ни одного нового пути вращения не
    заведено. Типизация нестандартного `GestureEvent`/`HTMLElementEventMap`
    — в новом `src/types/webkit-gestures.d.ts` (по образцу уже
    существующих `dat-gui.d.ts`/`tiles-data.d.ts` — свой файл на
    отдельный источник, не смешан с `geometry.d.ts`).

  **Решения, которые пришлось принять по месту (BACKLOG.md их явно не
  описывал):**
  1. Знак панорамирования по `deltaX`/`deltaY` — не копировался готовым
     (в `mapbox-gl-js` тачпад-свайп панорамированием не является вовсе —
     там классификация используется только для выбора СКОРОСТИ зума, а не
     для переключения зум/пан), выведен рассуждением и перепроверен
     численно: `deltaX`/`deltaY` `wheel`-события используют ту же
     конвенцию, что и обычный скролл страницы (положительное значение =
     "открыть больше контента в этом направлении"), а это тот же
     визуальный эффект, что и перетаскивание контента в
     противоположную сторону мышью — то есть тот же знак, что и у
     существующего mouse-drag пана, без дополнительной инверсии.
     Подтверждено Playwright-тестом (см. ниже): сумма `deltaX`/`deltaY` за
     серию из 5 событий (13 и 7 соответственно), делённая на текущий
     `zoom_factor` (=1 в момент теста), совпала с фактическим сдвигом
     `c.x`/`c.y` день в день (`Δc.x = 13`, `Δc.y = 7`).
  2. Знак поворота Safari-жеста — не в спецификации отдельно, выведен из
     уже установленной (`ZOOM-3`) конвенции: WebKit документирует
     положительный `.rotation` как поворот ПО часовой стрелке; чтобы
     физически по-часовой жест двумя пальцами поворачивал контент на
     экране по часовой (тот же результат, что уже проверен для
     тач-пинча), в `_drotation` дельта идёт с ВЫЧИТАНИЕМ, тем же
     рассуждением, что и в докстринге `rotateBy()`. Подтверждено прямым
     вызовом (см. ниже): синтетический `gesturechange` с `+45°` дал
     `_drotation = -π/4` ровно.
  3. Событие с `deltaX≠0, deltaY===0` (чистый горизонтальный свайп без
     вертикальной составляющей) классифицируется по `deltaY` (=0, как и
     у mapbox — `deltaX` в классификаторе не участвует), что при полном
     отсутствии вертикальной составляющей и большой паузе может один раз
     дать неопределённый `null` → зум по умолчанию → `_wheelZoom` с
     `dy=0` ничего не делает, событие "проглатывается". На практике
     реальный тачпад почти никогда не даёт `deltaY` РОВНО 0 при
     горизонтальном свайпе (есть остаточная вертикальная компонента) —
     известный, но не устранённый край, не описанный в исходном плане.
  4. `_wheel_grace_timer`/`setTimeout` на 40мс реализован, хотя
     сама ветвление "не определилось → зум" уже не зависит от него для
     ТЕКУЩЕГО события — он важен для КЛАССИФИКАЦИИ последующих событий
     той же паузы 40-400мс (без него `!state.type`-ветка эвристики
     продолжала бы срабатывать даже спустя сотни мс после одиночного
     щелчка мыши). Портирован ради точности, не просто "для галочки".

  **Проверено:**
  - `make typecheck` — чисто.
  - `make test-client` — 60/60 (было 52; добавлено 8 юнит-тестов на
    `classifyWheelEvent` в `map.test.ts`: одиночный "нотч" колеса →
    `'wheel'` независимо от таймингов; одиночное маленькое `deltaY` →
    `'trackpad'` сразу, даже первым событием жеста; серия
    маленьких/смешанных `deltaY` (2-6px) → `'trackpad'` на всей серии;
    структурная проверка, что классификатор не принимает `ctrlKey`
    вообще — эта развилка обрабатывается раньше, в `MapWidget`, а не
    внутри чистой функции; новый неоднозначный жест с паузой `>400`мс →
    `null`+`startGraceTimer`; быстрый повтор с малой/большой
    magnitude-per-ms после `null` → `'trackpad'`/`'wheel'` соответственно;
    нормализация `DOM_DELTA_LINE` (`deltaY*40`) даёт результат, идентичный
    эквивалентному "сырому" пиксельному значению).
  - `make build` — собрано, `docs/js/map.js`/`.js.map` обновлены.
  - **Playwright** (headless Chromium, локальный HTTP-сервер на
    свежем порту, собранная `docs/`-сборка; доступ к
    модуль-скоуп-переменной `map` — через `import('/js/map.js')` +
    monkey-patch `MapWidget.prototype.onRepaint`, с проверкой
    `this.container.id === 'workfield'`, чтобы не перехватить служебный
    тестовый инстанс, заведённый ниже для проверки жестов):
    синтетический `WheelEvent` с изолированным `deltaY`, кратным
    "нотч"-модулю (`100.006...`), без `ctrlKey` — `zoom_target` меняется,
    `c` не меняется; серия из 5 мелких `wheel`-событий с `deltaX`/`deltaY`
    (2-5px), без `ctrlKey` — `zoom_factor`/`zoom_target` НЕ меняются,
    `c.x`/`c.y` меняются ровно на `Σdelta/zoom_factor` (см. пункт 1 выше);
    `wheel` с `ctrlKey:true` — зумит, `_wheel_state`/`_wheel_last_time` не
    трогаются классификатором вовсе (подтверждает, что путь полностью в
    обход эвристики); новый неоднозначный жест с искусственно состаренным
    `_wheel_last_time` — зумит (дефолт), выставляет `_wheel_grace_timer`,
    который через 40мс сам финализирует состояние в `'wheel'`.
    Safari-жесты в Chromium дать нельзя (`GestureEvent` там не существует
    — подтверждено: `'ongesturestart' in window` === `false`,
    `typeof window.GestureEvent` === `'undefined'`) — вместо этого
    (а) подтверждено отсутствие ошибок/предупреждений в консоли на всей
    сессии (feature-detection действительно молча не навешивает
    слушатели), и (б) отдельный `MapWidget`, сконструированный при
    искусственно выставленном `window.ongesturestart = null` (тот же
    feature-detection срабатывает), получил синтетические (обычные
    `Event`, не настоящие `GestureEvent` — рантайм не проверяет тип,
    только читает `.rotation`) `gesturestart`(rotation=10) →
    `gesturechange`(rotation=55) → `gestureend` — `_drotation` изменился
    ровно на `-π/4` (см. пункт 2 выше), `gestureend` не бросил исключения.
  - **Известное фундаментальное ограничение, не устранено и не могло
    быть устранено этой правкой** (архитектурный факт Web Platform,
    подтверждён несколькими независимыми источниками при планировании):
    когда `ctrlKey`/`metaKey` ложны, 100%-надёжного различения мыши и
    тачпада не существует — браузер отдаёт одинаковые по форме события
    для обоих устройств, эвристика лишь минимизирует ошибку ценой
    небольшой систематической неопределённости на первом событии нового
    жеста (см. пункт 4 выше и BACKLOG.md-план).
  - **Поворот тачпадом работает только в Safari — это финальное решение,
    а не промежуточный шаг в ожидании поддержки Chrome/Firefox.** Там
    этого API не существует в принципе ни сейчас, ни в обозримом будущем
    (несколько независимых источников уже проверены на этапе
    планирования) — альтернатива для Chrome/Firefox (Shift+клик+
    перетаскивание одним пальцем по тачпаду, `ROT-1`) уже работает без
    единой новой строки кода, ничего дополнительно не потребовалось.
  - **Не проверено (и не может быть проверено в этой песочнице):**
    ощущение от реального железа MacBook — как быстро "выстреливает"
    momentum-скролл macOS в реальности, не режет ли что-то браузер по
    частоте `wheel`-событий на реальном трекпаде иначе, чем в синтетике.
    Требует подтверждения на реальном устройстве после мержа (тот же
    паттерн, что и у `ZOOM-10`).

  **Зависит от: ZOOM-9, ZOOM-10, ZOOM-11, ROT-6. Оценка: M.**

- [x] **ZOOM-13 — сделано.** По прямому запросу пользователя: "дабл тап
  с задержкой пальца на экране, после чего палец движется вверх/вниз —
  зум, влево/вправо — поворот". Стандартный мобильный паттерн (тот же,
  что у Google Maps для одного пальца, только Google не делает
  горизонталь поворотом — здесь это расширение по прямой просьбе).

  **Механика (расширяет `ZOOM-3`'s тач-обработчики, не отдельный
  фреймворк жестов — см. "Открытые вопросы" про то, когда он
  понадобится, а когда нет):**
  - Один палец, короткое касание (не более `TAP_MAX_DURATION_MS`,
    смещение при отпускании не больше `TAP_MAX_MOVEMENT_PX`) — обычный
    тап, запоминается как "ожидающий" (`_tap_pending`) с меткой времени
    и позиции.
  - Новый одиночный `touchstart` в пределах `DOUBLE_TAP_MAX_INTERVAL_MS`
    и `DOUBLE_TAP_MAX_DISTANCE_PX` от `_tap_pending` — это ВТОРОЙ тап
    двойного тапа. Вместо обычного (что сейчас происходит с любым
    одним пальцем) — **взводится** специальный режим
    (`_zoom_rotate_drag = 'armed'`), обычное панорамирование одним
    пальцем на это конкретное касание подавляется целиком, а не
    "пока не решим" — двойной тап уже достаточно однозначный сигнал
    намерения, чтобы не гадать дальше.
  - Как только палец сдвигается от точки второго тапа больше чем на
    `ZOOM_ROTATE_ACTIVATION_PX` (без прыжка — при пересечении порога
    начинается отсчёт заново от текущей точки, а не от исходной, чтобы
    не было скачка на весь пройденный путь разом) — режим становится
    `'active'`: якорь зума (`_zoom_anchor_screen`) фиксируется на точке
    ВТОРОГО тапа (тот же принцип, что у пинча/колеса — но тут это
    зафиксированная на весь жест точка, а не текущая позиция пальца,
    так же ведёт себя однопальцевый зум в Google Maps). Дальше на каждое
    смещение: вертикальная составляющая (`deltaY` со знаком — движение
    вверх = зум **внутрь**, вниз = зум **наружу**, тот же знак, что и в
    Google Maps) идёт в `zoomBy()` по формуле
    `2^(-Δy / ZOOM_DRAG_PX_PER_DOUBLING)` (сколько пикселей на удвоение
    зума — подобрано на глаз, как и `ROTATE_DRAG_SENSITIVITY` для
    Shift+drag, не измерено); горизонтальная составляющая — в
    `_drotation` напрямую по ТОЙ ЖЕ формуле и с ТЕМ ЖЕ
    `ROTATE_DRAG_SENSITIVITY`, что и у Shift+drag мышью (переиспользуется
    ради одинакового ощущения между вводами, не подбирается заново), с
    тем же знаком (сдвиг вправо = по часовой, как уже установлено для
    Shift+drag/`ZOOM-3`/`ZOOM-12`). Обе оси применяются независимо и
    одновременно (диагональное движение зумит И крутит сразу) — блокировки
    по одной оси нет, по прямому описанию запроса это не требовалось;
    если жест на практике будет ощущаться дёрганым — заводить отдельной
    задачей, не гадать заранее.
  - Второй палец, появившийся, пока режим `'armed'`/`'active'` —
    немедленно отменяет его, откатываясь к уже существующей
    двупальцевой обработке (`ZOOM-3`) без изменений.
  - Отпускание пальца в `'active'` — жест завершён штатно, состояние
    сбрасывается, это НЕ засчитывается как новый тап (был явный драг,
    не тап). Отпускание в `'armed'` без пересечения порога активации —
    "чистый" двойной тап без последующего драга — тоже просто
    сбрасывается, специального действия на него не назначено (не
    просили) и он не участвует в дальнейшем сопоставлении тапов.

  **Зависит от: ZOOM-3, ZOOM-9 (`zoomBy`), ROT-1 (`ROTATE_DRAG_SENSITIVITY`,
  `_drotation`). Оценка: M** (сама механика несложная, но требует
  аккуратной синтетической Playwright-проверки — реальный смартфон в
  headless-песочнице недоступен, тот же принцип, что и у `ZOOM-10`/
  `ZOOM-12` — плюс явной проверки, что обычное панорамирование одним
  пальцем и двупальцевые жесты не задеты регрессией).

  **Реализовано ровно по плану выше** (состояние `MapWidget._zoom_rotate_state:
  `null`/`'armed'`/`'active'`, `_zoom_rotate_touch_id`, `_zoom_rotate_start`/
  `_zoom_rotate_last`, плюс `_tap_pending`/`_single_touch_start` для
  тайминга самого тапа) — расширены существующие `touchstart`/`touchmove`/
  `touchend`/`touchcancel` из `ZOOM-3`, отдельного фреймворка жестов не
  заведено. Константы (все "на глаз", план явно предписывал не измерять):
  `TAP_MAX_DURATION_MS = 250`, `TAP_MAX_MOVEMENT_PX = 10`,
  `DOUBLE_TAP_MAX_INTERVAL_MS = 300`, `DOUBLE_TAP_MAX_DISTANCE_PX = 40`,
  `ZOOM_ROTATE_ACTIVATION_PX = 10`, `ZOOM_DRAG_PX_PER_DOUBLING = 300`
  (последняя — по прямой подсказке плана, "для единого ощущения с
  `ROTATE_DRAG_SENSITIVITY`'s ~300px на полный эффект").

  Знак поворота **не скопирован вслепую**, а перепроверен по факту:
  существующий Shift+drag пишет `_drotation += (old_x - e.pageX) *
  ROTATE_DRAG_SENSITIVITY` — то есть сдвиг вправо (`e.pageX` растёт) даёт
  ОТРИЦАТЕЛЬНУЮ дельту, а `rotation`, по докстрингу `rotateBy()`
  (найденному и исправленному ещё в `ZOOM-3`), поворачивает видимый
  контент ПРОТИВ часовой при РОСТЕ — то есть сдвиг вправо + отрицательная
  дельта = поворот контента ПО часовой. Новый код в touchmove
  использует ту же форму (`last.x - pos.x`, буквально "старое минус
  новое", как у мыши) — не переизобретён заново. Подтверждено и
  Playwright-тестом (см. ниже): синтетический сдвиг на 80px вправо дал
  `rotation = -80 * (π/300)` ровно.

  **Извлечены как чистые функции** (по образцу `classifyWheelEvent` из
  `ZOOM-12`) только три маленьких тайминг/дистанционных предиката:
  `isTap(durationMs, movementPx)`, `isDoubleTapContinuation(pending, now,
  pos)`, `hasCrossedActivationThreshold(start, pos)` — специально ради
  юнит-тестируемости без браузера. Остальная часть состояния — переходы
  `null → 'armed' → 'active'`, какой именно `Touch` отслеживается,
  выкачивание дельты в `zoomBy()`/`_drotation` — **осталась инлайном** в
  обработчиках: она неразрывно завязана на живые `Touch`/`TouchEvent` и
  остальные мутируемые поля `MapWidget` (`_zoom_anchor_screen`, сам
  `zoomBy()`, аккумулятор `_drotation`) — вытащить её в чистую функцию
  значило бы либо продублировать эти поля здесь же, либо протащить
  половину класса параметрами; такое извлечение выглядело бы
  искусственным ради самого извлечения, а не ради тестируемости — решено
  не форсировать, как и разрешал план.

  **Решения, которые пришлось принять по месту (план явно их не
  фиксировал):**
  1. **Цепочка из 3+ быстрых тапов.** План описывает только пару
     "тап1 → тап2". У тройного/более частого тапа: тап, который стал
     ВТОРЫМ тапом пары (взвёл жест — неважно, довёл ли он до `'active'`
     или был просто отпущен из `'armed'`), сам `_tap_pending` не
     оставляет — следующий (третий) тап начинает сопоставление с нуля, а
     не "довешивается" к первому. То есть сопоставляются только
     СОСЕДНИЕ пары подряд, ни один тап не может стать вторым для двух
     разных первых одновременно. Естественное следствие из "consumed"
     семантики `_tap_pending`/`_single_touch_start`, но план это не
     проговаривал явно.
  2. **`touchcancel` никогда не засчитывает тап**, даже если по
     длительности/смещению касание бы прошло `TAP_MAX_*`-пороги —
     трактуется как оборванный, а не завершённый контакт (план обсуждал
     `touchcancel` только для отмены `'armed'`/`'active'`-режима, не для
     обычного одиночного тапа — решено по аналогии).
  3. **Якорь зума** (`_zoom_anchor_screen`) выставляется в момент
     ВЗВОДА (`'armed'`, на touchstart второго тапа), а не в момент
     перехода в `'active'` — значение то же самое (позиция второго
     тапа не меняется между взводом и пересечением порога), так что
     это чисто эквивалентная перестановка, но по тексту плана
     ("фиксируется... когда режим становится `'active'`") можно было
     прочитать и как "выставлять именно на этом переходе"; решено не
     заводить лишнее поле ради временной точности, которая ни на что
     не влияет.
  4. **`_mouse_move_flag`/`_mouse_down_flag`** по-прежнему выставляются
     в 1 на touchstart второго тапа (общий код в начале обработчика,
     не специфичный для `ZOOM-13`) даже когда жест `'armed'`/`'active'`
     — панорамирование всё равно подавлено (`_dx`/`_dy` не
     накапливаются, ранний `return` в touchmove), так что единственный
     эффект — `scrollType: 'sliding'` каждый кадр обнуляет
     `_scroll_velocity` кандидатом `(0,0)` вместо реального движения;
     проверено, что это не создаёт остаточного дрейфа/наката после
     завершения зум/поворот-жеста.

  **Проверено:**
  - `make typecheck` — чисто.
  - `make test-client` — 83/83 (было 70; добавлено 13 юнит-тестов —
    `isTap`/`isDoubleTapContinuation`/`hasCrossedActivationThreshold` —
    каждый пограничный случай на самой границе своей константы, `<=`
    против `<`/`>` против `>=`, а не просто "явно внутри"/"явно
    снаружи").
  - `make build` — собрано, `docs/js/map.js`/`.js.map` обновлены.
  - **Playwright** (headless Chromium, локальный HTTP-сервер на свежем
    порту, собранная `docs/`-сборка; доступ к module-scope `map` — тот же
    приём, что и в `ZOOM-3`/`ZOOM-12`: `import('/js/map.js')` +
    monkey-patch `MapWidget.prototype.onRepaint` с проверкой
    `this.container.id === 'workfield'`; синтетические `Touch`/
    `TouchEvent`, диспетчеризуемые напрямую на канвас):
    - Одиночный тап-и-отпускание сам по себе не меняет `zoom_target`/
      `rotation`/`c` и не переводит `_zoom_rotate_state` из `null`
      (только кладёт себя в `_tap_pending`, что и требуется для
      возможного будущего второго тапа).
    - Два тапа (30мс удержание, 80мс между ними, второй в 2-3px от
      первого) с последующим движением ВВЕРХ на второй руке — довели
      состояние `'armed' → 'active'`; `zoom_target` вырос ровно на
      `2^(80/300)` (движение на активирующем перепаде в 18px, как и
      предписано планом, эффекта не дало — расчёт по фактически
      применённым 80px совпал с результатом день-в-день), `rotation`
      не изменился НИ НА БИТ, `c` сдвинулся только за счёт уже
      существующей ZOOM-1 anchor-drift коррекции (якорь — точка
      второго тапа, не центр экрана).
    - То же, но движением ВПРАВО — `rotation` изменился ровно на
      `(420-500)*(π/300)` (420 — точка пересечения порога активации, а
      не исходная точка тапа 402 — это заодно подтвердило и "не
      прыгать на пройденный путь при пересечении порога" отдельно от
      самого факта поворота), `zoom_target` не изменился совсем, `c`
      остался `(0,0)` (при неизменном зуме anchor-коррекции неоткуда
      взяться).
    - Диагональное движение (вправо+вверх одновременно) — `zoom_target`
      И `rotation` изменились одновременно за один и тот же жест,
      подтверждая отсутствие блокировки по одной оси.
    - Тапы с разрывом в 1с (при том же расстоянии) и с разрывом в 200px
      (при той же задержке) — оба НЕ взвели жест
      (`_zoom_rotate_state` остался `null`); второе касание в обоих
      случаях отработало как обычное панорамирование одним пальцем —
      `c` сдвинулся ровно на `(-Δx/zoom_factor, -Δy/zoom_factor)`, как
      и у любого обычного драга.
    - Второй палец, коснувшийся экрана в момент `'active'` —
      `_zoom_rotate_state` мгновенно стал `null`, исключений не
      брошено; последующий полноценный двупальцевый жест (панорама по
      центроиду + пинч по отношению расстояний + поворот по углу)
      отработал корректно на тех же самых пальцах — регрессии в
      `ZOOM-3`'s двупальцевой обработке нет.
    - Отпускание в `'armed'` без пересечения порога активации
      (покачивание пальца в пределах 5px) — `zoom_target`/`rotation`/`c`
      не тронуты,
      состояние сброшено в `null`, новый тап не зарегистрирован;
      следующая, никак не связанная попытка двойного тапа сразу после
      этого взвелась и снялась штатно — состояние ничем не испорчено.
    - Отдельно — обычный однопальцевый драг без единого тапа рядом
      по-прежнему даёт `c`, смещённый ровно на
      `(-Δx/zoom_factor, -Δy/zoom_factor)`, `zoom_target`/`rotation` не
      тронуты — поведение `ZOOM-3` для этого пути не задето вообще.
  - **Не проверено (и не может быть проверено в этой песочнице):**
    ощущение жеста на реальном сенсорном экране — подобраны ли
    константы (особенно `TAP_MAX_DURATION_MS`/`DOUBLE_TAP_MAX_*`/
    `ZOOM_ROTATE_ACTIVATION_PX`) достаточно щедро/строго для настоящего
    пальца, а не таймера в тесте. Тот же класс ограничения, что и у
    `ZOOM-10`/`ZOOM-12` — требует подтверждения на реальном устройстве
    после мержа.

  **Follow-up (по прямому запросу пользователя: "нужно сделать, чтобы
  не включались одновременно и вращение и масштабирование").** Именно
  тот случай, который исходный план сознательно оставил "не гадать
  заранее, завести отдельной задачей, если жест окажется дёрганым" —
  задача заведена. Касается только этого, однопальцевого жеста
  (`ZOOM-13`) — двупальцевый пинч+поворот (`ZOOM-3`) НЕ трогается: там
  одновременные зум и поворот — стандартное, ожидаемое поведение
  (Google Maps/Apple Maps/Mapbox все позволяют крутить и щипать
  одновременно двумя пальцами), пользователь жаловался не на него, и
  распространять axis-lock туда было бы неверным домыслом за пределы
  прямого запроса.

  **Механика:** в момент пересечения порога активации (переход
  `'armed' → 'active'`) сравнить `|Δx|` и `|Δy|` ИМЕННО этого
  пересекающего порог смещения (а не последующих) и **зафиксировать**
  ось на весь оставшийся жест — не мышление заново на каждый кадр
  (постоянная переоценка доминирующей оси при движении по диагонали
  рядом с 45° давала бы дёрганое перескакивание между зумом и
  поворотом туда-обратно, что как раз и надо исключить). Ничья
  (`|Δx| === |Δy|`) — в пользу зума (вертикаль — базовый жест, поворот
  — расширение поверх него по этому же запросу изначально). Дальше в
  `'active'`-ветке применяется ТОЛЬКО зафиксированная ось —
  `zoomBy()`/`_drotation`-обновление для другой оси просто не
  вызывается, а не подавляется постфактум. **Оценка: S** (точечное
  изменение уже существующей ветки `'active'`, не новая инфраструктура).

  **Сделано, ровно по плану выше.** Добавлено одно новое поле состояния
  `MapWidget._zoom_rotate_axis: 'zoom' | 'rotate' | null`, инициализируется
  в конструкторе и сбрасывается в `null` в ТЕХ ЖЕ трёх местах, что уже
  сбрасывали `_zoom_rotate_state`/`_zoom_rotate_touch_id` (переход
  `'armed' → 'active'` не сбрасывает — ось выставляется именно там;
  сброс — на конце жеста в `touchend`/`touchcancel` и на немедленной
  отмене вторым пальцем в `touchstart`), так что между жестами ничего не
  протекает. Само решение оси — прямо в момент пересечения порога, по
  `|pos.x - _zoom_rotate_start.x|` против `|pos.y - _zoom_rotate_start.y|`
  ИМЕННО этого пересекающего смещения (не следующих), ничья — в `'zoom'`.
  В `'active'`-ветке — два независимых `if` по значению
  `_zoom_rotate_axis` вместо прежних безусловных `if (dy !== 0)`/
  `if (dx !== 0)`: при `'zoom'` `_drotation` вообще не трогается (ветка
  `if (dx !== 0) this._drotation += ...` физически не выполняется), при
  `'rotate'` — симметрично не вызывается `zoomBy()`. Никакой новой
  инфраструктуры/констант не добавлено — оценка `S` подтвердилась.

  **Проверено:**
  - `make typecheck` — чисто.
  - `make test-client` — 83/83, без изменений (ни один существующий тест
    этот код не задевает — `isTap`/`isDoubleTapContinuation`/
    `hasCrossedActivationThreshold` остались чистыми функциями, не
    тронуты).
  - `make build` — собрано, `docs/js/map.js`/`.js.map` обновлены.
  - **Playwright** (тот же приём, что и у самого `ZOOM-13`: локальный
    HTTP-сервер на `docs/`, `import('/js/map.js')` +
    monkey-patch `MapWidget.prototype.onRepaint` по `container.id ===
    'workfield'`, синтетические `Touch`/`TouchEvent` на канвас, реальные
    `requestAnimationFrame`-кадры между событиями — иначе `_drotation`
    не долетает до `rotation`, у него нет своего пути без кадра
    анимации, в отличие от `zoom_target`, который `zoomBy()` меняет
    синхронно):
    - Преимущественно вертикальное пересекающее движение (`|Δy|=15 >
      |Δx|=2`) заперло ось на `'zoom'`; три следующих движения, КАЖДОЕ с
      одновременно ненулевыми `x`- и `y`-составляющими
      (`(+40,+20)`, `(-60,+25)`, `(+30,-10)`), дали `zoom_target`
      `1 → 0.9548… → 0.9013… → 0.9223…` (меняется на каждом шаге), а
      `rotation` — константа, побитово равна значению на момент входа в
      `'active'` (`0`), все три кадра подряд.
    - Преимущественно горизонтальное пересекающее движение (`|Δx|=15 >
      |Δy|=2`) заперло ось на `'rotate'`; те же три диагональных
      движения дали `rotation`
      `0 → -0.4189 → 0.2094 → -0.1047` (меняется на каждом шаге), а
      `zoom_target` — константа `0.9223…` (то самое значение, на котором
      остановился предыдущий, зум-жест) все три кадра подряд.
    - Пересекающее движение с точной ничьей (`Δx = Δy = 12`px, оба выше
      порога активации в 10px) заперло ось на `'zoom'`, как и требует
      правило ничьей.
    - **Проверка утечки между жестами:** зум-жест (заперт на `'zoom'`) →
      отпускание пальца → `_zoom_rotate_axis` сразу `null` (вместе с
      `_zoom_rotate_state`) → немедленно новый двойной тап с
      преимущественно горизонтальным движением → заперся на `'rotate'`,
      без единого следа от предыдущего `'zoom'`.

- [x] **ZOOM-14 — сделано.** Усилить чувствительность зума при pinch-жесте на
  тачпаде (`ctrlKey`/`metaKey` на `wheel`, `ZOOM-12`) — по прямому
  отчёту пользователя: "на маке при жесте щипком на тачпаде
  масштабирование работает, однако очень слабо... прям в разы усилить".
  Причина: pinch-ветка (`e.ctrlKey || e.metaKey`) и обычное колесо мыши
  сейчас идут через ОДИН И ТОТ ЖЕ `_wheelZoom()` с ОДНОЙ И ТОЙ ЖЕ
  формулой, откалиброванной под величину `deltaY` физического "нотча"
  колеса мыши (~100, `WHEEL_DELTA_PER_STEP`) — а pinch-жест repoртит на
  порядок меньшие `deltaY` за событие (по независимым источникам,
  собранным при планировании `ZOOM-12` — единицы, не сотни), так что та
  же формула даёт для pinch исчезающе малый шаг за событие.

  **Важно про формулировку "только на маке, на смартфонах не трогай":**
  буквальной привязки к ОС (User-Agent sniffing) не будет и не нужно —
  `ctrlKey`/`metaKey` на `wheel`-событии уже ОДНОЗНАЧНО обозначает
  именно pinch-жест тачпада (или физический Ctrl+колесо; см. `ZOOM-12`)
  и НИКОГДА не возникает ни у обычного колеса мыши, ни (тем более) у
  тач-пинча на смартфоне — тот идёт через полностью отдельный механизм
  (`ZOOM-3`, отношение расстояний между пальцами, вообще не через
  `wheel`). То есть усиление именно этой ветки уже автоматически и
  единственно верно "только на маке" (и на Windows Precision Touchpad
  таким же жестом — тоже уместно, не проблема) и не задевает ни мышь,
  ни смартфон, безо всякого определения платформы.

  **План:** `_wheelZoom(e)` получает необязательный параметр
  чувствительности (по умолчанию `1`, для колеса мыши — поведение не
  меняется), pinch-ветка вызывает его с новой константой
  `PINCH_ZOOM_SENSITIVITY` (множитель на `deltaY` до того, как он войдёт
  в уже существующую clamped-proportional формулу `ZOOM-9`/`ZOOM-10`, —
  переиспользуется целиком, не копируется). Значение — "на глаз", по
  прямой просьбе "в разы" (не измерено, реального тачпада в песочнице
  нет — тот же класс ограничения, что и у `ZOOM-10`/`ZOOM-12`/`ZOOM-13`),
  с явным комментарием, что можно смело подстраивать дальше после теста
  на реальном железе. Существующий `WHEEL_DELTA_PER_STEP`-кламп
  сохраняется как есть поверх уже умноженного `deltaY` — это заодно
  ограничивает МАКСИМАЛЬНЫЙ разовый скачок от резкого/быстрого щипка тем
  же потолком, что и один нотч мыши, что разумно как защита от резкого
  скачка сама по себе, а не только побочный эффект.

  **Зависит от: ZOOM-9, ZOOM-10, ZOOM-12. Оценка: S** (один параметр +
  одна константа поверх уже готовой формулы, не новая механика).

  **Реализовано ровно по плану.** `_wheelZoom(e: WheelEvent, sensitivity:
  number = 1)` — `sensitivity` умножает `dy` (`-e.deltaY`) ДО того, как
  он попадает в существующий `clamped_dy`/`WHEEL_DELTA_PER_STEP`-кламп и
  `Math.pow(1 + zoom_step_factor, ...)`-формулу (`ZOOM-9`/`ZOOM-10`) —
  сама формула и кламп не тронуты ни на строчку. `ctrlKey || e.metaKey`-
  ветка теперь зовёт `this._wheelZoom(e, PINCH_ZOOM_SENSITIVITY)`;
  соседний вызов (`'wheel'`/неопределившийся `classifyWheelEvent`) —
  `this._wheelZoom(e)`, без изменений, дефолт `1` не даёт никакой разницы
  для мыши.

  **Значение `PINCH_ZOOM_SENSITIVITY = 10`.** На глаз, как и
  предполагалось планом (реального тачпада в песочнице по-прежнему нет —
  тот же класс ограничения, что у `ZOOM-10`/`ZOOM-12`/`ZOOM-13`).
  Рассуждение: независимые источники, собранные при `ZOOM-12`
  (`classifyWheelEvent`'s `Math.abs(value) < 4` → "точно тачпад", и
  отдельная цитата про типичный тачпад-жест — `deltaY` 2-6 за событие)
  сходятся на том, что реальный pinch репортит единицы за событие — то
  есть без усиления `dy=3` даёт `clamped_dy/100 = 0.03` в показателе
  степени формулы, практически нулевой шаг за событие. `×10` поднимает
  тот же показатель до `0.3` — заметный, но не мгновенно
  "телепортирующий" шаг за одно событие, и всё ещё далеко от клампа
  (`clamped_dy` дошёл бы до потолка только при исходном `|deltaY| ≥ 10`,
  что для pinch — уже быстрый/резкий жест, а не типичный). Учитывая, что
  реальный pinch — плотный поток событий за жест (не одно), а не
  одиночный "щелчок" мыши, накопленный эффект за весь жест будет
  ощутимо/"в разы" сильнее прежнего при том же физическом движении
  пальцев, как и просил пользователь, не переходя в "телепорт" даже на
  резком рывке (из-за уже существующего клампа). Свободно
  перенастраивается (это ровно одна константа) после проверки на
  реальном железе.

  **Проверено:**
  - `make typecheck`, `make test-client` — все существующие 83 теста (6
    файлов) прошли без изменений (для этого не потребовалось новых
    unit-тестов — чистая параметризация уже протестированной формулы).
    `make build` — чисто.
  - **Playwright** (тот же приём, что у `ZOOM-3`/`ZOOM-12`/`ZOOM-13`:
    локальный статический сервер на `docs/`, `import('/js/map.js')` +
    monkey-patch `MapWidget.prototype.onRepaint` по `container.id ===
    'workfield'` для получения ссылки на живой инстанс). Три проверки, с
    численным сравнением, не "на глаз":
    1. **Прямой вызов `_wheelZoom` с/без sensitivity** (изолированное
       сравнение, `zoom_target` сбрасывается в `0.5` перед каждым
       вызовом, вдали от `zoom_min`/`zoom_max`, чтобы `ZOOM-5`-смягчение
       границы не участвовало): одинаковый `deltaY=3` (реалистичная
       величина одиночного pinch-события) — без sensitivity даёт
       `zoom_target/0.5 = 0.994545…` (`ln` = `-0.0054696…`); с
       `sensitivity=10` — `0.946772…` (`ln` = `-0.054696…`). Отношение
       логарифмов (в экспоненциальной формуле именно оно линейно по
       `dy·sensitivity`, а не сам множитель) — `10.000000000000062`,
       то есть усиление ТОЧНО в `PINCH_ZOOM_SENSITIVITY` раз, как и
       требует план (без клампа — при этом `dy`, `30 < 100`, кламп ещё
       не участвует).
    2. **Сквозной прогон через настоящий `wheel`-listener**
       (`canvas.dispatchEvent(new WheelEvent('wheel', {deltaY: 3,
       ctrlKey: true, ...}))`, без прямого вызова `_wheelZoom`) дал ТОЧНО
       тот же `zoom_target`, что и прямой вызов
       `_wheelZoom(e, PINCH_ZOOM_SENSITIVITY)` из проверки (1) —
       подтверждает, что `ctrlKey`-ветка действительно передаёт
       константу, а не просто существует в коде отдельно от рантайма.
    3. **Регресс для обычной мыши — сквозной прогон.** Одиночное
       `wheel`-событие `deltaY=100, ctrlKey: false` через настоящий
       listener (значит, и через `classifyWheelEvent`) дало
       `zoom_target/0.5 = 0.833333…`, что ТОЧНО равно `1/(1+
       zoom_step_factor) = 1/1.2` — тот же самый эталонный шаг одного
       нотча мыши, что и в `ZOOM-9`/`ZOOM-10`. Регресса нет.

- [x] **KEY-1 — сделано.** По прямому отчёту пользователя: "горячие
  клавиши привязаны к раскладке языка, а надо чтобы они работали
  независимо от включённого языка". Причина: `_keysDown`
  (WASD/стрелки/Z/X/Q/E) индексировался по
  `KeyboardEvent.key.toLowerCase()` — символу, который РЕАЛЬНО
  печатает активная раскладка на этой физической клавише, а не по
  самой клавише. На русской ЙЦУКЕН физическая "W" даёт `key: 'ц'`, а не
  `'w'` — сравнение с `DEFAULT_PAN_UP_KEYS` молча никогда не совпадало.
  Дискретные `+`/`-`/`[`/`]` были подвержены тому же на практике: код
  уже проверял `.code` в дополнение к `.key`, но сами массивы по
  умолчанию содержали только символы (`'+'`, `'['` и т.п.), а не
  соответствующие `.code`-значения (`'Equal'`, `'BracketLeft'`), так
  что проверка `.code` была фактически мертва.

  Исправлено переключением на `KeyboardEvent.code` — физическое
  положение клавиши, не зависящее от раскладки/языка: `_keysDown`
  теперь хранит `.code`, все `DEFAULT_PAN_*`/`DEFAULT_*_HOLD_KEYS`
  переписаны в `.code`-значения (`'KeyW'`, `'KeyA'`, ...), а к
  дискретным массивам (`zoomInKeys` и т.д.) добавлены реальные
  `.code`-значения (`'Equal'`, `'Minus'`, `'BracketLeft'`,
  `'BracketRight'`) рядом со старыми символьными — старое поведение
  (кастомный массив через опции в стиле `.key`) не ломается, проверка
  идёт по обоим. **Проверено через Playwright:** синтетическое событие
  с `key: 'ц', code: 'KeyW'` (имитация русской раскладки) по-прежнему
  распознаётся как "вверх"; дискретная клавиша зума с произвольным
  `.key`, но `code: 'Equal'` по-прежнему зумит.

- [ ] **SRC-1** — Явный интерфейс `TileSource`: сейчас смешаны
  синхронный `onGet` и асинхронный паттерн `state:'prepare'` +
  `makeReadyCallback` (`src/tile_source.ts:129-135`). Ввести
  Promise-based контракт `get(x, y, z): Promise<Tile>` с адаптером для
  старого callback-стиля, чтобы новые источники не обязаны были
  реализовывать ad hoc состояние-машину. **Оценка: M.**
- [x] **SRC-2 — сделано, без ожидания `SRC-1`.** Три класса добавлены в
  `src/tile_source.ts`: `XYZTileSource` (URL-шаблон + retry из `SRC-4`,
  перенесённый сюда из `layers.ts`-локального `makeTileGetter` —
  retry-с-backoff не специфичен для конкретного демо-источника),
  `TMSTileSource` (инверсия Y при построении URL, `x`/`z` не трогает —
  адресация тайлов для кэша/`heat()` остаётся в XYZ-конвенции), и
  `StaticCanvasTileSource` (по образцу xkcd-источника). Все пять
  источников в `layers.ts` (`tsMerged`/`tsBack`/`tsFront`/`tsOSM`/
  `tsStrava`) переведены на `XYZTileSource`; xkcd-источник — на
  `StaticCanvasTileSource`. `TMSTileSource` реализован, но никуда не
  подключён — в этом проекте нет ни одного реального TMS-источника,
  проверен только юнит-тестом на инверсию `y`.

  **Найдена и исправлена настоящая регрессия, привнесённая при
  проектировании `StaticCanvasTileSource`:** первая версия API
  (`drawTile(ctx, x, y, z): boolean`) всегда создавала полноразмерный
  canvas (2048×2048 у xkcd) ДО вызова `drawTile`, тогда как оригинальный
  xkcd-код проверял наличие данных и выходил без единой аллокации.
  Итог — на разреженных данных (большинство запросов мимо) каждый промах
  стал стоить целую 2048×2048 canvas-аллокацию впустую. Исправлено:
  `drawTile(getCtx, x, y, z): boolean` — `getCtx()` лениво создаёт canvas
  при первом вызове, так что "нет данных" по-прежнему ничего не
  аллоцирует. **Поймано не осмотром кода, а Playwright-тестом** —
  включил xkcd-слой и специально ушёл на позицию/зум, где почти все
  запросы мимо (мимо не потому что баг, а потому что xkcd и OSM-слои
  используют независимые системы координат — см. ниже), сравнил "было
  чёрным экраном" / "стало нормальной картой".

  **Побочная (не блокирующая) находка, отдельная от этой регрессии:**
  при экстремальном синтетическом zoom-out (26 шагов колеса в один тик)
  с ОДНОВРЕМЕННО активным xkcd-слоем канвас в этой стеснённой по памяти
  песочнице **необратимо чернел** даже ПОСЛЕ фикса выше (когда лишняя
  аллокация уже не происходит) — и не восстанавливался даже спустя 20с
  после того, как fps/dt вернулись в норму; свежий `reload` страницы
  чинит всё немедленно. Похоже на потерю canvas-контекста браузером под
  памятью — в кодовой базе нет обработчика `contextlost`/
  `contextrestored` вообще (это не новая, а ранее существовавшая
  прореха, просто раньше не было повода её задеть). Не блокирует
  коммит — заведено отдельной задачей **SRC-2-contextloss** ниже.
  - **SRC-2-contextloss** — Добавить `canvas.addEventListener('contextlost'/'contextrestored', ...)`
    в `MapWidget`, чтобы потеря контекста хотя бы не оставляла страницу
    молча чёрной навсегда (минимум — залогировать/показать сообщение;
    по возможности — переинициализировать состояние канваса при
    восстановлении). Замечено только при искусственно жёстком
    синтетическом стресс-тесте (10 слоёв + 26 zoom-шагов в один тик) в
    памяти-ограниченной среде — не обязательно воспроизводится на
    обычной машине при обычном использовании. **Оценка: S-M.**

  3 юнит-теста в `src/tile_source.test.ts` на `buildUrl`/TMS-инверсию —
  без обращения к `Image()`/`document` (недоступны в Node-окружении
  vitest по умолчанию; сетевой путь `XYZTileSource.get()` проверен
  отдельно через Playwright на реальных 400 в `SRC-4`). **Зависит от:
  SRC-1. Оценка: M.**
- [ ] **SRC-3** — Понятие `TilingScheme`, отдельное от `TileSource`:
  `tileSize`, `zoomFactorToLevel(zf): number`, границы уровня, что
  разводит взаимозависимость "зум как степень двойки" (сейчас `z_max`
  слоя — по сути произвольный сдвиг для xkcd-схемы, смешанный с
  вычислением `Math.ceil(Math.log2(zf))` в `getLevelParams`,
  `src/map.ts:344`). Готовит почву под неоднородные схемы (EPSG:3857
  Slippy vs. плоские игровые карты). **Зависит от: AFF-4 (переезжает
  туда же, где переписывается `getLevelParams`). Оценка: M.**
- [x] **SRC-4 — сделано, без ожидания `SRC-1`.** `makeTileGetter`
  (`src/layers.ts`) теперь ловит `img.onerror` и повторяет попытку с
  экспоненциальным backoff (по умолчанию 1с/2с/4с, до 3 повторов —
  настраивается через новый `MakeTileGetterOptions`); после исчерпания
  попыток — `tile.state = 'error'` и `console.warn` с URL вместо тихого
  зависания в `'prepare'` навсегда. Один и тот же объект `Tile`
  переиспользуется на всех попытках (мутируются только
  `preparing_image`/`state`) — значит успешный повтор "самоисцеляет" уже
  закэшированную в `TSCache` запись без каких-либо изменений в самом
  кэше. **Решено не ждать `SRC-1`:** оказалось, что этот фикс не
  требует предварительного Promise-based контракта — текущая модель
  "prepare → mutate in place → ready/error" уже прекрасно на него
  ложится. **Проверено через Playwright** на реальных 400-ошибках
  внешнего тайл-сервера (в этой песочнице их и так много): за ~10с
  видны и повторные попытки, и итоговое `"Tile load failed permanently
  after 4 attempt(s): <url>"` ровно один раз на тайл — ни зависания, ни
  дублирующего спама.
  **Отдельно, не сейчас:** таймер повтора не отменяется, если тайл
  выпал из зоны интереса/вытеснен из кэша раньше, чем сработал (см.
  `LOAD-4`) — не растущая утечка (обновляет уже никем не читаемый
  объект и завершается), но лишняя работа; решать вместе с `LOAD-4`.
  **Оценка: S-M** (совпала с исходной).
- [ ] **SRC-5** — Поддержка авторизованных источников (заголовки,
  токены): текущий путь `new Image().src = url` не даёт передать
  заголовки — добавить вариант загрузки через `fetch` + `Blob URL` для
  источников, которым это нужно. **Зависит от: SRC-1. Оценка: M.**
- [ ] **SRC-6 (решено: виджет — отдельная переиспользуемая библиотека)** —
  Декларативная конфигурация источников (JSON/объект вместо захардкоженных
  констант `tsMerged/tsBack/tsFront/tsOSM/tsStrava` в `src/layers.ts`),
  чтобы виджет переиспользовался без правки исходников под конкретный
  проект — это прямое следствие решения делать виджет библиотекой, а не
  частью только этого демо-сайта. **Зависит от: SRC-2. Оценка: M.**
- [ ] **SRC-7** — Per-layer `opacity`/composite-режим при смешивании
  нескольких тайловых слоёв (`TiledLayer.draw` сейчас не знает про
  прозрачность и порядок блендинга, кроме порядка в массиве
  `ALL_LAYERS`). **Зависит от: AFF-4. Оценка: S.**
- [ ] **SRC-8** — Генеративный даунсемплинг-враппер над `TileSource`
  ("mip-уровни" по образцу растровых пирамид): по прямому запросу
  ("добавить несколько менее детальных уровней зума для XKCD: уменьшить
  в 2, 4, 8, 16 раз"). Проблема шире одного XKCD-слоя:
  `StaticCanvasTileSource`-источники, у которых `onGet` физически
  игнорирует `z` (как у `xkcd_tiles` в `src/layers.ts` — ключ строится
  только из `x`/`(64-y)`), отдают один и тот же нативный тайл независимо
  от того, насколько мелко он в итоге отрисуется на экране: при
  сильном отдалении виджет вынужден просить и растягивать вниз множество
  полноразмерных (2048×2048) тайлов, а не меньшее число заранее уменьшенных
  — архитектурно лишняя нагрузка и (что и заметил пользователь) не тот
  внешний вид, что у настоящей тайловой пирамиды. Решение — обёртка
  `DownsampledTileSource` (`src/tile_source.ts`, рядом с
  `StaticCanvasTileSource`): оборачивает базовый источник и его нативный
  уровень `z0`; `get(x, y, z)` при `z < z0` строит один составной тайл,
  рисуя `n = 2^(z0-z)` × `n` тайлов базового источника (индексы от
  `x*n` до `x*n+n-1`, аналогично по `y`) в уменьшенном масштабе на
  канвасе `tile_size`, кэшируя результат как обычный `Tile` (переиспользует
  `TSCache`, никакой отдельной логики хранения не нужно). Для `z >= z0`
  — прозрачный проброс к базовому источнику без изменений (уровни детальнее
  нативного — не эта задача, см. "буст масштаба"/`LAYER-6`+"Открытые
  вопросы"). Общее решение, не привязанное к XKCD — годится для любого
  источника с фиксированной глубиной пирамиды. **Зависит от: SRC-2.
  Оценка: M.**

## Фаза 4. Фоновая загрузка и предиктивная предзагрузка тайлов (`LOAD-*`)

- [x] **LOAD-1 (Фаза 0) — сделано.** `TiledLayer.draw` теперь сам вызывает
  `tile_source.heat()` для любого источника, у которого этот метод есть
  (`TSCache`, через новый дак-тайпинг-гард `isHeatableTileSource` в
  `src/tile_source.ts`), с радиусом на основе видимой области текущего
  кадра слоя. Уточнение по факту проверки: `heat()` до этой правки не
  вызывался вообще нигде в кодовой базе (не только закомментированный
  путь в `index.ts` — прежняя формулировка аудита про "вручную для
  xkcd-локаций" была неточной, `defines.ts`-локации `heat()` тоже не
  звали). Заодно исправлена опечатка в дефолте `zdn` в `TSCache.heat()`
  (`src/tile_source.ts`, раньше по умолчанию брал значение `zup`).
  **Обновление — найден и исправлен настоящий, более серьёзный баг,
  дремавший в коде с самого начала (именно потому, что `heat()` нигде
  не вызывался):** колбэк в `TSCache.heat()` складывал `x/y/z` ЕЩЁ РАЗ
  с уже абсолютными координатами, которые ему передают `square`/`ring`/
  `heat` (`src/tile_tree.ts`) — те и так отдают абсолютные координаты, а
  не смещения. В реальном использовании это удваивало тайл-индекс
  (`x=315412` вместо `157706`) и уводило `z` далеко за пределы любой
  реальной тайловой пирамиды (`z=36` вместо `18`) — настоящий тайл-сервер
  корректно отвечал на это `400 Bad Request`. Кроме того, у `heat()` был
  ВТОРОЙ независимый баг: дефолтное исследование "соседних Z-уровней"
  (`zup`/`zdn`, было `1`) держит тот же `x,y` при переходе на `z±1` — это
  осмысленно только для z-независимой адресации (xkcd, где `z` вообще не
  участвует в поиске данных), но ломает любую обычную XYZ-пирамиду (там
  на каждом уровне зума своя сетка, тот же `x,y` на `z-1` — это ДРУГОЙ,
  никак не связанный тайл). Дефолт `zup`/`zdn` изменён на `0` (искать
  только на том же Z) — xkcd это не задевает функционально (просто
  раньше он бесполезно кэшировал одни и те же данные под двумя лишними Z),
  ничего специфичного для xkcd не тронуто. **Оба бага найдены не
  осмотром кода, а по прямому запросу пользователя** ("вижу много 400
  ошибок, откуда они?") — тестовым инструментированием прямо в браузере
  (см. `SendMessage`/сессионные заметки), поскольку ручная трассировка
  рекурсии `heat()`/`ring()`/`square()` заводила в тупик несколько раз
  подряд. Тест на регрессию — `src/tile_source.test.ts`. **Итог на
  практике:** 172 ложных 400-ошибки в консоли на дефолтном виде → 0
  (остаётся только безобидный `favicon.ico` 404).
  **Обновление — корректный счётчик тайлов в отладочной информации:**
  `TSCache` получил геттеры `loaded_count`/`error_count`/`loading_count`
  (первый — по наличию `tile.image`, второй — по `tile.state === 'error'`
  после исчерпания ретраев `SRC-4`, третий — то, что осталось: в очереди
  или в полёте), а `TiledLayer` — поле `visible_tile_count`, выставляемое
  в `draw()` по фактическому размеру видимого диапазона
  (`(2*dx+1)*(2*dy+1)`). `drawDebugInfo` (`src/layers.ts`) теперь показывает
  для переднего (OSM) слоя отдельную строку
  `tiles(front): visible=... loading=... loaded=... error=... cached=...`
  — раньше в отладке была только сумма `cache_size` по всем источникам,
  безымянная, приклеенная к строке fps. Строка вынесена в две `fillText`
  (не влезает в отведённую ширину одной строкой при `20px Arial`), с
  расширенным левым отступом (`w - 520`, было `w - 420`, тоже мало —
  клипало `error=.../cached=...` о правый край канваса — поймано
  Playwright-скриншотом, не расчётом ширины текста заранее).
- [ ] **LOAD-2** — Приоритетная очередь вместо стека: `load_queue` в
  `TSCache` (`src/tile_source.ts:39,52`) — это `Array` с `.pop()` (LIFO,
  без приоритетов). Переписать на структуру с приоритетом
  (видимые тайлы > первое кольцо предзагрузки > дальний прогрев),
  например бинарную кучу или несколько раздельных очередей по уровню
  приоритета. **Зависит от: LOAD-1. Оценка: M.**
- [ ] **LOAD-3** — Бюджет по времени кадра вместо тика "1 тайл / 10 мс"
  (`onBackgroundHeat`, `src/tile_source.ts:50-57`): планировать через
  `requestIdleCallback` (с фолбэком на `setTimeout`) и ограничивать не
  количеством тайлов, а миллисекундами, которые фоновая загрузка может
  занять до следующего кадра. **Зависит от: LOAD-2. Оценка: S-M.**
- [ ] **LOAD-4** — Отмена запросов, покинувших зону интереса: для
  fetch-based источников (`SRC-5`) — `AbortController`; для
  `Image()`-based — сброс `img.onload/onerror` и `img.src = ''`. Раньше
  отменять было нечего — тайлы либо в кэше, либо в очереди без
  отслеживания "актуальности". **Появился конкретный кандидат из
  `SRC-4`:** таймеры повтора после `img.onerror` (`setTimeout` в
  `makeTileGetter`) сейчас не отменяются, если тайл покинул зону
  интереса раньше, чем сработал повтор. **Зависит от: LOAD-2, SRC-1.
  Оценка: M.**
- [x] **LOAD-5 — сделано (LRU, не LFU).** `TSCache.storage` теперь `Map`
  вместо обычного объекта — при каждом чтении ключ удаляется и
  вставляется заново, что бесплатно двигает его в конец порядка
  итерации `Map` (гарантированно = порядок вставки), поэтому "самый
  старый" ключ всегда лежит первым, без отдельной бухгалтерии. При
  вставке новой записи, если размер превышает `cache_limit`
  (настраивается через `TileSourceOptions.cache_limit`, по умолчанию
  2000 — просто разумное число, не тонко подобранное), вытесняется
  ключ с начала. `cache_size` стал геттером над `storage.size` вместо
  вручную инкрементируемого счётчика — иначе он разъехался бы с
  реальностью, как только появилось вытеснение (счётчик, который
  никогда не уменьшается, но записи пропадают, — гарантированный баг).
  LFU (по частоте, не по свежести) не делался — не было оснований
  предпочесть его LRU для этого паттерна доступа (соседние тайлы при
  панорамировании). Заодно 6 юнит-тестов в `src/tile_source.test.ts`
  (`TSCache` не имеет DOM-зависимостей, тестируется напрямую): кэширование
  `Tile`/`null`, **не**-кэширование `undefined` (чтобы не залипать на
  временном "нет ответа"), вытеснение по LRU и то, что чтение продлевает
  жизнь записи. **Зависит от: LOAD-1. Оценка: M.**
- [ ] **LOAD-6** — Предзагрузка по направлению движения: расширять кольцо
  прогрева в сторону вектора скорости панорамирования (уже считается как
  `_scroll_velocity`, `src/map.ts:50`), а не только симметричным кольцом
  вокруг текущей точки, как сейчас в `heat()`/`ring()`/`square()`
  (`src/tile_tree.ts`). **Зависит от: LOAD-2. Оценка: M.**
- [ ] **LOAD-7** — Прогрев с учётом поворота вьюпорта (эллиптическая/
  повёрнутая зона интереса вместо квадратного кольца) — угол поворота
  уже есть (`ROT-1`/`ROT-3`), это про сам прогрев. Естественно решается
  вместе с `ROT-3-range` (та же проекция углов канваса через обратную
  матрицу). **Зависит от: LOAD-6, ROT-3-range. Оценка: M.**
- [x] **LOAD-8 — сделано.** По прямому запросу пользователя: "расширь
  дебаг-статистику по тайлам: сколько запрошено с сервера, сколько
  получено, сколько взято из кеша, сколько закешировано, сколько тайлов
  запрошено заранее, сколько тайлов в рамках экрана". До этого оверлей
  показывал только *живые* срезы (`loaded_count`/`error_count`/
  `loading_count`, из `LOAD-1`) и `cache_size` — не было видно ни
  суммарного трафика к источнику, ни эффективности кэша/прогрева.

  Добавлены четыре кумулятивных (за всю жизнь страницы) счётчика в
  `TSCache` — переживают вытеснение LRU, в отличие от геттеров-снимков:
  `requested_count` (промахи мимо кэша, `get()`), `cache_hit_count`
  (попадания в кэш, тот же метод), `received_count` (успешно
  догрузившиеся тайлы — через уже существовавший `onReady`-колбэк
  `Tile.makeReadyCallback`, дёрнутый из `XYZTileSource.fetchTile`'s
  `img.onload`), `prefetch_queued_count` (тайлы, поставленные в очередь
  `heat()`'ом, `LOAD-1`). Блок дебаг-оверлея стал тремя строками: живое
  состояние вьюпорта (`screen`/`inflight`/`loaded`/`error`), состояние
  кэша (`cached` — снимок, плюс три новых кумулятивных счётчика) и
  прогрев (`prefetch` кумулятивно + `pending` — живая длина очереди).
  Левый отступ расширен (`w-560`, было `w-520`) — кумулятивные счётчики
  только растут, `hits` за долгую сессию может уйти в миллионы.

  Назван `LOAD-8`, не `LOAD-7` — тот уже зарезервирован в бэклоге под
  другую, нереализованную задачу (учёт поворота при прогреве).

## Фаза 5. Вращение вьюпорта (`ROT-*`)

- [x] **ROT-1 — сделано (изначально без touch-пинча — добавлен позже в
  `ZOOM-3`).** `Shift+drag` мышью вращает напрямую (без сглаживания, как
  и панорамирование — решается один раз при `mousedown` по
  `e.shiftKey`, не переключается на лету при удержании); клавиши `[`/`]`
  — шаг на 5° каждая, `Home` — сброс на север; все три идут через тот же
  `dt`-based easing, что и `zoom_factor` (переиспользован тот же `alpha`,
  не заведена вторая независимая постоянная времени). Слайдер в dat.GUI
  для наглядности/точного тестирования. Touch двумя пальцами теперь
  тоже вращает (см. `ZOOM-3`); явные кнопки "+N°" в UI — не сделаны,
  этого можно добросить отдельно позже, когда/если понадобится.

  По ходу найден и исправлен реальный баг: расход накопленной за кадр
  дельты поворота (`_drotation`) был завязан на **живой** флаг
  "перетаскивание ещё активно", а не на факт "дельта накопилась в этом
  кадре" — из-за этого быстрый жест (mousedown→move→mouseup до
  следующего кадра рендера — обычное дело для синтетического теста, не
  невозможное и для настоящего быстрого клика) молча терял накопленный
  поворот. Исправлено по образцу панорамирования (`_dx`/`_dy` расходуются
  безусловно, без привязки к живому флагу).

  Заодно исправлен смежный баг: глобальный `keydown`-хендлер (зум и
  теперь поворот) перехватывал нажатия, даже когда фокус был в текстовом
  поле dat.GUI — например, ввод "-" в числовое поле раньше попутно
  вызывал `zoomOut()`. Добавлена проверка `e.target` на `INPUT`/
  `TEXTAREA`/`SELECT`.

  **Проверено через Playwright:** синтетический `Shift+drag` на 300px
  дал ровно ожидаемые 180° (карта отрисовалась вверх ногами, тайлы
  по-прежнему бесшовны); `Home` плавно вернул к исходной, пиксель-в-пиксель
  совпадающей с дефолтным видом, ориентации; 6 нажатий `]` дали ровно
  30°, что отразилось в URL (см. `ROT-2`).

  **Обновление — непрерывное управление на удержании клавиш: `WASD`
  (панорамирование) + `Q`/`E` (поворот), в дополнение к `Z`/`X` (зум,
  см. `ZOOM-4`).** По прямому запросу пользователя ("не понятно как
  крутить карту... сделай биндинг на w,a,s,d / z,x / q,e"). Общий
  механизм для всех шести клавиш: `_keysDown: Set<string>` заполняется
  на `keydown`/`keyup` (после того же `INPUT`/`TEXTAREA`/`SELECT`-гарда,
  что и раньше — держит игровые клавиши от dat.GUI-полей), плюс
  `window.addEventListener('blur', ...)` очищает набор целиком, чтобы
  клавиша не "залипала" зажатой, если фокус ушёл из окна во время
  удержания (Alt+Tab, клик в другую вкладку) — без этого `keyup` мог бы
  никогда не прийти. Опрашивается не по одному нажатию, а на каждом
  кадре `onRepaint`, через `frame_dt` (тот же клампленный dt, что и
  зум/поворот-easing, — см. `ZOOM-2`), поэтому скорость не зависит от
  fps. `WASD` идёт напрямую через `scroll()` (не через drag-аккумулятор
  `_dx`/`_dy` — у них противоположная знаковая конвенция), скорость
  `KEYBOARD_PAN_SPEED_PX_PER_SEC = 600` экранных px/сек, делённая на
  `zoom_factor` — та же конвертация "экранные px → мировые единицы",
  что и у драга (`this._dx /= this.zoom_factor` перед `scroll()`),
  поэтому реальный экранный сдвиг не зависит от текущего зума. Диагональ
  (например `W+D`) нормализуется (`Math.hypot`), чтобы не быть быстрее
  осевого движения. `Q`/`E` пишут прямо в `_drotation` — тот же
  аккумулятор, что расходует `Shift+drag`, то есть поворот немедленный,
  как драг, а не через сглаживаемый `rotation_target`, которым управляют
  дискретные `[`/`]`.

  **Проверено через Playwright** (dispatch синтетических
  `KeyboardEvent('keydown'/'keyup')`, замер по значениям в
  dat.GUI-слайдерах `Zoom`/`Angle (rad)` и по `pos=` в отладочном
  оверлее — считан через временный перехват `CanvasRenderingContext2D.
  prototype.fillText`, т.к. `map` не выставлен в `window`): `d` 200мс дал
  сдвиг мировых координат ровно в сторону, ожидаемую для "камера едет
  вправо" (контент на экране уехал влево на ~130px, ожидалось ~120px);
  `q`/`e` наглядно повернули сцену в противоположные стороны; `z`/`x`
  задокументированы в `ZOOM-4`; удержание `d` с последующим `blur` вместо
  `keyup` остановило панорамирование немедленно (позиция не менялась ни
  сразу после `blur`, ни спустя ещё 600мс) — залипания клавиши нет.
- [x] **ROT-2 — сделано.** `update_url_position` пишет третий компонент
  — угол в целых градусах (человекочитаемо в адресной строке),
  `src/index.ts` парсит его обратно в радианы; старые двухкомпонентные
  ссылки по-прежнему работают (группа поворота в регулярном выражении
  опциональна, поворот тогда просто 0). **Попутная находка и
  исправление:** до этой правки регулярное выражение (`/#\[(\d+),(\d+)\]/`)
  не поддерживало **отрицательные** `x`/`y` вообще — мировые координаты
  меньше нуля никогда не восстанавливались бы из URL корректно; добавлен
  `-?` для всех трёх групп. **Проверено через Playwright:** URL после
  поворота на 30° — `#[40373076,22579095,30]`; свежая загрузка страницы
  именно с этим URL восстанавливает и позицию, и угол; старый
  двухкомпонентный URL (`#[12482409,27045819]`) по-прежнему грузится без
  ошибок.
- [x] **ROT-3 — решение пересмотрено по итогам практической проверки:
  офф-скрин буфер не нужен.** Изначальный план (см. ниже, историческая
  запись) исходил из того, что каждый тайл придётся поворачивать
  по отдельности, из-за чего на стыках была бы щель. Но к моменту, когда
  дошли до реализации, `AFF-4` уже переписал отрисовку тайлов через ОДНУ
  общую матрицу на весь слой за кадр (`computeTileGridMatrix` +
  `ctx.setTransform`) — а значит все тайлы слоя поворачиваются
  **одним и тем же** аффинным преобразованием, и проблема
  несовпадающего антиалиасинга на стыках попросту не возникает (это
  ровно то же самое, почему соседние тайлы и без поворота никогда не
  давали щели — единая непрерывная математика на весь слой). Проверено
  эмпирически (`camera.rotation` выставлен вручную на 23° и на 45°,
  скриншот через Playwright) — тайлы карты стыкуются идеально бесшовно
  на обоих углах, без всякого буфера. Реальная работа ROT-3 свелась к
  тому, что уже сделано в `ROT-1` (включить вращение камеры через
  пользовательский ввод) — как отдельная задача ROT-3 закрыта, буферный
  план не реализовывался.

  Из проверки на 45° остались два реальных, но не блокирующих,
  наблюдения — не часть ROT-3 самой по себе, вынесены отдельно:
  - Видимый диапазон тайлов (`tx/ty/dx/dy` в `getLevelParams`) всё ещё
    считается для identity-трансформации (см. `AFF-4`) — в тесте на 45°
    пустых клиньев по краям не появилось (текущий запас в эвристике
    оказался на практике достаточным для этого конкретного зума/размера
    тайла), но это везение конфигурации, не гарантия. Честная версия
    (проекция четырёх углов канваса через обратную матрицу) всё ещё
    стоит сделать для надёжности — заведено как **ROT-3-range** ниже.
  - При повороте на 45° fps просел (33fps против обычных 55-60,
    в стеснённой по памяти песочнице) — вероятно, из-за того, что
    больше тайлов покрывает диагональ И каждый рисуется через отдельный
    `ctx.save()/setTransform()/restore()` (решение `AFF-4`, чтобы не
    трогать контракт `onTileDraw`). Не мерено строго, не факт, что
    воспроизводится на обычной машине — заведено как **ROT-3-perf** ниже,
    не блокирует.
  - [x] **ROT-3-range — сделано.** По прямому отчёту пользователя ("при
    ненулевом повороте... не прорисовываются тайлы по краям из-за
    некорректного определения области видимости") подтвердилось именно
    то "везение конфигурации", о котором предупреждала запись выше —
    на другом зуме/размере канваса пустые клинья по краям при повороте
    были уже хорошо видны. `getLevelParams` переписан ровно так, как
    планировалось: `tx/ty/dx/dy` теперь считаются проекцией четырёх
    углов канваса через обратную `computeTileGridMatrix` (та же матрица,
    что и реальную отрисовку тайлов — гарантированно согласовано с тем,
    что действительно рисуется, включая per-layer shift/scale), взятием
    bounding box в тайл-индексных координатах плюс запас в 1 тайл на
    случай, когда угол канваса попадает внутрь, а не точно на границу
    тайла. `getLevelParams` заодно строит и возвращает саму `gridMatrix`
    (раньше `draw()` строил её отдельно после) — один расчёт на слой за
    кадр вместо двух. **Проверено через Playwright:** на 0°/15°/45°/80°
    все четыре угла канваса лежат внутри выбранного диапазона
    `[tx-dx,tx+dx]×[ty-dy,ty+dy]` (проверено обратным преобразованием),
    `dx/dy` растут с (4,3) при 0° до (5,5) при 45°, как и ожидается от
    инфляции bounding box повёрнутого прямоугольника; скриншот на 45° и
    на -51° — тайлы покрывают канвас от края до края, пустых клиньев
    нет.
  - **ROT-3-perf** — При необходимости (если реальные измерения покажут
    проблему, не просто песочница) вынести `ctx.setTransform` из
    расчёта на тайл в расчёт на слой (один `save`/`setTransform`/
    `restore` на все тайл-изображения слоя за кадр, отдельным проходом
    от `onTileDraw`-коллбэков) — набросок такого прохода уже обсуждался
    при реализации `AFF-4`, не взят тогда ради меньшего диффа. **Оценка:
    S, приоритет низкий — сначала измерить на реальном железе.**

  <details><summary>Историческая запись: изначальный (нереализованный) план буфера</summary>

  Тайлы рисуются мозаикой в неповёрнутых мировых координатах в отдельный
  офф-скрин канвас-буфер; на экран буфер переносится одним `drawImage` с
  применённым поворотом. Составляющие, которые обсуждались: размер
  буфера с запасом на диагональ; dirty-flag инвалидация (полная
  перерисовка при смене позиции/зума/тайлов, но не при чистом повороте);
  прогрев/загрузка с запасом на диагональ буфера; сглаживание при
  финальном `drawImage`. Не реализовано — см. решение выше.
  </details>
- [x] **ROT-4 (часть "сетка должна вращаться") — сделано.** По прямому
  отчёту пользователя ("при ненулевом повороте неправильно
  отрисовывается сетка") — `map_grid`'s `onTileDraw` (`src/layers.ts`)
  рисовал оси-выровненный `ctx.rect(x, y, tsize, tsize)` в пиксельных
  координатах: сама позиция ячейки была верной (`x/y` уже приходили из
  `gridMatrix`), но её ФОРМА никогда не поворачивалась вместе с камерой
  — отсюда "рваная" на вид сетка при ненулевом повороте, тогда как
  соседние тайл-изображения (рисуемые через `ctx.setTransform` на
  `gridMatrix`) уже поворачивались как единый жёсткий лист.
  `onTileDraw` теперь получает саму `gridMatrix` восьмым параметром
  (проведена через `TiledLayer.tileDraw`, добавлена в
  `TiledLayerOptions['onTileDraw']`) и сетка обводит РЕАЛЬНЫЕ четыре
  угла ячейки — `gridMatrix.transformPoint({x: ix±0/1, y: iy±0/1})` — в
  экранных пиксельных координатах, а не через `ctx.setTransform` на
  саму `gridMatrix`.

  **Важная находка по пути:** первая версия рисовала именно через
  `ctx.setTransform(gridMatrix...)` + единичный квадрат в
  тайл-индексных координатах — тот же приём, что уже работает для
  изображений тайлов в `tileDraw()`. На глубоком зуме (тайл-индексы
  порядка 10^5–10^6) это молча рисовало ПУСТОЕ МЕСТО: линия шириной
  ~1 экранный пиксель после масштабирования матрицей (~250×) требует в
  локальных координатах смещения контура на ~0.002-0.004 единицы, а на
  величине координат ~150000+ это меньше зазора между соседними
  представимыми числами `float32` — растровизатор канвы (Skia) считает
  геометрию обводки во `float32`, и это смещение округляется в 0,
  контур вырождается в пустоту. Подтверждено эмпирически: обводка
  толщиной в несколько единиц (после масштабирования — сотни экранных
  пикселей) через тот же `setTransform` рисовалась нормально, "тонкая"
  (~1px) — не рисовалась вообще, при абсолютно идентичных
  `ctx.globalAlpha`/`strokeStyle`/матрице (залогировано через
  monkey-patch `CanvasRenderingContext2D.prototype.stroke`). Урок:
  `ctx.setTransform` на матрицу с большим смещением/масштабом безопасен
  для `drawImage`/`fillRect` (геометрия примитива не зависит от толщины
  линии), но НЕ безопасен для тонких `stroke()` — там geometry-объезд
  считается в локальном пространстве, и трансформировать точки самому
  перед вызовом `moveTo`/`lineTo` (как сделано сейчас) — правильный
  приём. **Проверено через Playwright:** на 45° и на -51° сетка образует
  чистую повёрнутую решётку без разрывов и "разлохмаченных" углов, во
  всех проверенных случаях видна от края до края канваса.

  Билборд-часть исходной задачи (подписи/координатные метки
  `drawTileDebug`/`drawDebugInfo` НЕ должны вращаться вместе с картой)
  пользователем не поднималась на тот момент и не была тронута — заведена
  отдельно как **ROT-4-billboard** ниже, не блокирует.
  - [x] **ROT-4-billboard — сделано (для `drawTileDebug`), попутно с
    отдельным отчётом по `MapTilesDebug`.** По прямому отчёту
    пользователя ("посмотри слой MapTilesDebug, он сейчас поломан так же
    как поломана была сетка") — `drawTileDebug` (общая функция для
    `xkcd_debug` и `map_debug`/"MapTilesDebug") страдала ровно тем же
    багом, что и `map_grid` до своего фикса выше: оси-выровненный
    `ctx.rect(x, y, tsize, tsize)` (плюс второй, "ужатый" внутренний
    rect) на верной позиции, но с неверной (не поворачивающейся) формой
    — "рваная" рамка при ненулевом повороте. Исправлено тем же приёмом:
    четыре угла ячейки проецируются через `gridMatrix` и обводятся одной
    линией — без лишнего внутреннего rect'а, который не нёс полезной
    информации. Подпись `[ix, iy]/iz` при этом сознательно оставлена
    НЕ повёрнутой (правильный billboard — читаемой при любом повороте
    карты), но теперь позиционируется в РЕАЛЬНОМ экранном центре ячейки
    (`gridMatrix.transformPoint({x: ix+0.5, y: iy+0.5})`), а не по старой
    оси-выровненной формуле `(x + tsize/2, y + tsize/2)`, верной только
    при повороте 0. Отдельный флаг `billboard: boolean` не понадобился —
    решение "не вращать, но позиционировать верно" оказалось
    единственным разумным для debug-подписи, вводить конфигурируемость
    было не за что.
- [x] **ROT-6 — сделано.** По прямому отчёту пользователя: "при повороте
  карты управление остаётся привязано к старому положению, то есть при
  повороте на 180 градусов оно инвертируется". `scroll(dx, dy)` и
  анти-дрейфовая коррекция зум-якоря (`ZOOM-1`) обе трактовали свой
  экранно-ориентированный вектор так, будто это уже мировая дельта, и
  прибавляли его прямо к `c` — верно только при повороте 0. Как только
  камера повёрнута, "экранный вправо" перестаёт быть "мировым +x", и
  ЛЮБОЙ путь панорамирования через `scroll()` (drag мышью во всех трёх
  `scrollType`, WASD/стрелки) оставался привязан к исходным,
  неповёрнутым осям: при повороте на 180° каждое направление выходило
  инвертированным относительно того, что реально на экране, а на любом
  другом угле — со сдвигом на этот угол. У коррекции зум-якоря был тот
  же баг по той же причине (она отменяет дрейф экранной точки-якоря при
  зуме), просто не так заметный на глаз, как перевёрнутый WASD.

  Исправлено поворотом вектора на `this.rotation` (через
  `Mat2D.rotation`, та же конвенция, что и везде в коде) перед
  прибавлением к `c`, в обоих местах. Режим скролла `sliding` прибавлял
  скорость напрямую (`this.c.add(...)`), в обход `scroll()` — теперь
  тоже идёт через `scroll()`, вместо четвёртого, рассинхронизированного
  пути. **Проверено численно:** `scroll(10, 0)` ("вправо" на экране)
  теперь даёт мировую дельту `(10, 0)` при повороте 0°, `(0, 10)` при
  90°, `(-10, 0)` при 180°, `(0, -10)` при 270° — то есть всегда именно
  то мировое направление, которое на экране в данный момент реально
  выглядит как "вправо".
- [ ] **ROT-5** — Компас/индикатор севера как overlay-слой поверх карты,
  использующий тот же `Transform2D` для получения текущего угла; клик по
  компасу — плавный сброс поворота в 0 (готовый `MapWidget.resetRotation()`
  из `ROT-1` уже делает именно это, останется только вызвать). **Зависит
  от: ROT-1. Оценка: S.**

## Фаза 6. Вложенные вьюпорты ("viewport-in-viewport") (`VP-*`)

- [ ] **VP-1** — Формализовать дерево трансформаций: `MapWidget` —
  корневой `Transform2D`/камера, каждый `Layer` — узел, у узла могут быть
  дочерние вьюпорты со своей независимой позицией/зумом/поворотом внутри
  системы координат родителя. **Зависит от: AFF-2..AFF-4. Оценка: M**
  (в основном оформление API поверх уже готового `AFF-*`).
- [ ] **VP-2** — API создания дочернего вьюпорта:
  `viewport.createChild(transform): Viewport`, независимая обработка
  pan/zoom/rotate внутри дочерней системы координат, с корректной
  проекцией указателя (курсор должен транслироваться в локальные
  координаты вложенного вьюпорта через `screenToLocal` дочернего узла, а
  не родительского). **Зависит от: VP-1, ZOOM-3. Оценка: M.**
- [ ] **VP-3** — Клиппинг области дочернего вьюпорта (`ctx.save()` +
  `ctx.clip()` по границе врезки) и остановка propagation указателя между
  уровнями — жест внутри врезки не должен одновременно панорамировать
  родителя. **Зависит от: VP-2. Оценка: M.**
- [ ] **VP-4** — Демо: миникарта (overview) как дочерний вьюпорт с
  обратной связью — клик/драг по миникарте двигает главный вьюпорт, и
  наоборот, рамка на миникарте отражает текущую область родителя.
  **Зависит от: VP-3. Оценка: M.**
- [ ] **VP-5** — Тесты композиции 2-3 уровней вложенности (расширение
  `AFF-5` на реальные `Viewport`-объекты, а не голые `Transform2D`).
  **Зависит от: VP-2, INFRA-1. Оценка: S.**

## Фаза 7. Векторные слои (`VEC-*`)

Решено сразу проектировать под потоковые тайловые векторные данные (MVT/
vector tiles), а не только статический GeoJSON — см.
[Принятые решения](#принятые-решения).

- [ ] **VEC-1** — Класс `VectorLayer extends Layer` + минимальная
  GeoJSON-совместимая модель геометрии (`Point`/`LineString`/`Polygon`/
  `MultiPolygon`), рисуемая через матрицу трансформации слоя (та же, что
  у `AFF-4`) — без ручного пересчёта координат вручную, как сейчас
  устроено в `TiledLayer`. Модель фичи должна одинаково подходить и для
  "целого" набора данных (нетайловый источник, `VEC-3a`), и для набора
  фич одного тайла (`VEC-3b`), т.е. фича не обязана знать, из какого
  источника она пришла. **Зависит от: AFF-4. Оценка: M.**
- [ ] **VEC-2** — Стилизация: `fill`/`stroke`/`width`/`dash`/иконки-маркеры,
  data-driven `style(feature)` (как в Leaflet/OpenLayers), zoom-зависимая
  стилизация (скрывать мелкие детали на малом приближении).
  **Зависит от: VEC-1. Оценка: M.**
- [ ] **VEC-3a** — `VectorSource`: базовый контракт источника (согласован
  с `SRC-1`) + простой статический адаптер (готовый GeoJSON в
  памяти/по URL) — нужен как минимум для тестов/демо и для оверлеев без
  своей тайловой сетки (например, один маршрут поверх карты). **Зависит
  от: VEC-1, SRC-1. Оценка: S.**
- [ ] **VEC-3b** — Тайловый векторный источник (MVT/Mapbox Vector Tile,
  protobuf): декодирование по адресации `z/x/y`, использующей ту же
  `TilingScheme` (`SRC-3`), что и растровые источники. Требует
  npm-зависимость для декодирования protobuf/MVT (например связка
  `@mapbox/vector-tile` + `pbf`, либо минимальный собственный парсер,
  если тащить целую библиотеку избыточно — сравнить в короткой заметке
  перед реализацией). **Зависит от: VEC-3a, SRC-3. Оценка: L.**
- [ ] **VEC-3c** — Решение по стыкам между тайлами: MVT-геометрия обычно
  приходит уже обрезанной по границе тайла (так делают все стандартные
  генераторы MVT) — рендерим как есть, без попытки сшивать соседние
  тайлы в одну фичу (это стандартная практика в MapLibre/Mapbox GL,
  избавляет от комбинаторной сложности сшивки на лету). Явно
  задокументировать это ограничение и то, как оно сказывается на
  `VEC-4` (см. ниже). **Зависит от: VEC-3b. Оценка: S** (в основном
  решение + документирование, не код).
- [ ] **VEC-4** — Hit-testing и интерактивность: определение фичи под
  курсором (point-in-polygon, расстояние до линии) с анпроекцией
  экранных координат клика в мировые координаты слоя через
  `screenToLocal`; события `onFeatureClick`/`onFeatureHover`. Для
  тайлового источника (`VEC-3b`/`VEC-3c`) проверять фичи текущего тайла
  под курсором — клик у самой границы тайла может не задеть фичу,
  обрезанную по этой границе в соседнем тайле; это известное и
  принятое ограничение MVP, не задача для отдельного решения. **Зависит
  от: VEC-1, AFF-3. Оценка: M.**
- [ ] **VEC-5** — Пространственный индекс (grid/quadtree) для отсечения
  фич вне видимой области перед проходом по геометрии — нужен только для
  нетайлового источника (`VEC-3a`) с большим числом фич. Для тайлового
  источника (`VEC-3b`) сама тайловая сетка уже является пространственным
  индексом (отсечение происходит на уровне выбора видимых `z/x/y`, как у
  растровых тайлов) — отдельный индекс избыточен. **Зависит от: VEC-3a.
  Оценка: M.**
- [ ] **VEC-6** — Подписи фич в режиме billboard (переиспользовать флаг
  из `ROT-4`). **Зависит от: VEC-1, ROT-4. Оценка: S.**
- [ ] **VEC-7** — Демо-слой с тайловыми векторными данными (например,
  границы/дороги в формате MVT) поверх текущих растровых тайловых слоёв,
  для проверки MVP на практике. **Зависит от: VEC-1..VEC-4. Оценка: S.**
- [ ] **VEC-8** — Редактирование геометрии на карте, по прямому запросу
  ("возможность создавать и редактировать геометрии геоданных на
  карте"): режим `editable` на фиче — хендлы на вершинах (billboard-
  маркеры, `VEC-6`), драг вершины двигает её в реальном времени
  (hit-test уже есть — `VEC-4`), клик на середину сегмента добавляет
  вершину, модификатор-клик (например Alt+клик) удаляет её; отдельный
  режим "рисование новой геометрии" — цепочка кликов, двойной клик/Enter
  завершает, Escape отменяет. События (`feature:editstart`/
  `vertexadd`/`vertexmove`/`vertexremove`/`editend`) через `EVT-*`.
  Самая объёмная задача во всём бэклоге — полноценный geometry editor,
  а не просто отображение. **Зависит от: VEC-4, EVT-1. Оценка: L.**
- [ ] **VEC-9** — Undo/redo для правок геометрии — тонкая обёртка над
  событиями `VEC-8` (стек снапшотов геометрии), не полноценный
  command-pattern на весь виджет. **Зависит от: VEC-8. Оценка: S.**
- [ ] **VEC-10** — Кластеризация точечных фич на малом зуме (когда точки
  визуально слипаются) — нужно только при больших объёмах точечных
  данных, не входит в MVP. **Зависит от: VEC-1. Оценка: M, приоритет
  низкий.**

## Аудит: от прототипа к виджету

По прямому запросу — куда двигаться, чтобы прототип стал полноценным
переиспользуемым картографическим виджетом. Ниже — то, чего сейчас **нет
вообще** (в отличие от исходного [аудита](#аудит-текущего-состояния),
который фиксировал баги в том, что уже было) — каждый пункт разворачивается
в фазу `8-14` ниже:

- Нет никакой единой событийной модели: у `MapWidget` есть только два
  разовых колбэка через опции конструктора (`onLocate`, `onZoom`,
  `src/map.ts`) — ни подписки нескольких обработчиков, ни событий тайлов
  (загрузка/выход из вида/вытеснение), ни разделения "живого" перемещения
  от "конечного" (с учётом инерции/easing). См. `EVT-*`.
- Нет проекций/координатных систем: мировые координаты — это "глобальный
  пиксель на самом глубоком уровне тайловой пирамиды" (`zoom_level_max`),
  без формул перевода в/из WGS84 (lon/lat) — а именно в WGS84 приходят
  реальные GPS-треки, `Geolocation API`, GeoJSON "из внешнего мира". Без
  этого векторные слои (`VEC-*`) годятся только для синтетических/уже
  спроецированных данных. См. `PROJ-*`.
- Слои — фиксированный плоский массив (`ALL_LAYERS`, собранный руками в
  `src/index.ts`), без менеджера (add/remove/reorder), без прозрачности
  на уровне слоя (`Layer.opacity`) и без слоя, который просто рисует себя
  функцией без обращения к тайловому кешу — то есть оверлей, формирующийся
  на лету по событиям, сейчас пришлось бы городить как fake-`TiledLayer`.
  См. `LAYER-*`.
- Геоданные (точки/линии/полигоны) вообще не отображаются: `Фаза 7`
  (`VEC-*`) на момент этого бэклога не начата — есть только тайловые
  растровые/debug-слои. Стилизация, hit-testing и — по прямому текущему
  запросу — редактирование геометрии добавлены в `VEC-*` (`VEC-8..VEC-10`
  выше).
- Сериализация состояния в URL — только позиция и поворот, только по
  двойному клику (`update_url_position`, `ROT-2`), без зума, без общего
  API "сериализовать/восстановить состояние" отдельно от URL как
  конкретного носителя, и без систематической live-записи по ходу
  панорамирования/зума. См. `STATE-*`.
- Переход "от точки к точке" одним прыжком не поддержан: `zoom_target`/
  `rotation_target` — независимые друг от друга "магниты", каждый едет
  своим темпом при простом присваивании: собрать из этого управляемый
  по времени перелёт между двумя полными состояниями вьюпорта (позиция+
  зум+поворот согласованно, с кривой easing и событиями) сейчас нечем.
  См. `FLY-*`.
- Размер виджета реагирует только на `window.onresize` (`src/map.ts`,
  `onResize`) — не на изменение размеров самого контейнера (сайдбар,
  флекс/грид-перестроение), и через `window.onresize =`, а не
  `addEventListener` (перезаписывает чужой обработчик, а не
  сосуществует с ним). См. `RESIZE-*`.
- Динамических/движущихся объектов нет вообще: ни растущего трека, ни
  своей позиции по `Geolocation`, ни виртуального объекта, едущего по
  траектории с заданными параметрами. См. `DYN-*`.

## Фаза 8. Событийная модель (`EVT-*`)

Решено следовать уже устоявшемуся в экосистеме нейминг-паттерну
(Leaflet/MapLibre: `map.on('move', ...)`, `movestart`/`move`/`moveend`,
`zoomstart`/`zoom`/`zoomend`) — не изобретать заново, а использовать то,
что уже знакомо разработчикам, которые придут в проект. См. также
[Принятые решения](#принятые-решения).

- [x] **EVT-1 — сделано.** Класс `EventEmitter<EventMap>` в
  `src/event_emitter.ts`: `on`/`once`/`off`/`fire`, без внешних
  зависимостей. Типизация — через параметр-словарь "имя события ->
  форма payload'а" (`EventMap extends Record<string, unknown>`), так что
  конкретный потребитель (будущие `MapWidget.on('move', ...)` и т.п. из
  `EVT-2`/`EVT-3`) получает проверку типов и автодополнение на свои
  события, а не `any`. `on`/`once` возвращают функцию отписки — удобно
  для инлайновых обработчиков без сохранённой ссылки. **Композиция, а не
  миксин/базовый класс:** `MapWidget`, `Layer` и `TileSource` — три
  независимые иерархии без общего предка, у каждой уже (или по `LAYER-*`/
  `SRC-*`) появятся свои причины для базового класса; отдельное поле
  (`private _events = new EventEmitter<...>()`) с парой
  тонких forwarding-методов даёт то же самое переиспользование без траты
  единственного `extends`-слота и без навязывания всем трём одинаковой
  формы публичного `on`/`off` (обоснование — в комментарии к файлу).
  **`off()` во время `fire()`:** `fire()` итерируется по снимку
  (`list.slice()`), снятому до вызова обработчиков, а не по живому
  массиву — отписка (в т.ч. `once()`, который отписывает сам себя первым
  делом) внутри обработчика не сдвигает индексы под текущим циклом и не
  пропускает/не дублирует соседние обработчики; эффект виден только на
  следующем `fire()`. Покрыт 10 юнит-тестами в
  `src/event_emitter.test.ts` (vitest): базовый `on`/`fire`, несколько
  слушателей на одно событие, `off()` снимает конкретный обработчик (не
  все), `once()` срабатывает ровно один раз и отписывается сам,
  отписка себя и отписка соседа изнутри обработчика во время `fire()`,
  `fire()` без слушателей — не падает, независимость списков слушателей
  разных событий, передача payload'а. `make typecheck` и
  `make test-client` (62/62, включая уже существовавшие 52) — зелёные;
  `make build` собрал `docs/js/event_emitter.js`.
- [ ] **EVT-2** — События жизненного цикла вьюпорта с разделением "живого"
  и "конечного" состояния (по прямому запросу): `movestart`/`move`/
  `moveend`, `zoomstart`/`zoom`/`zoomend`, `rotatestart`/`rotate`/
  `rotateend`. "start" — на первое реальное изменение после состояния
  покоя; "move"/"zoom"/"rotate" — каждый кадр, пока значение фактически
  меняется (включая доводку инерцией/easing — событие живёт дольше, чем
  сам ввод пользователя); "end" — когда значение перестало меняться (тот
  же приём, что уже применяется для отсечки доводки зума,
  `Math.abs(target - factor) < eps`). Отдельно — событие о смене
  НАМЕРЕНИЯ без ожидания фактического окончания анимации
  (`zoomtargetchange` и т.п.) — стреляет сразу при изменении
  `zoom_target`/`rotation_target`, до физической доводки. **Зависит от:
  EVT-1. Оценка: M.**
- [ ] **EVT-3** — События жизненного цикла тайла, по прямому перечню
  запроса, пробрасываемые из `TSCache`/`TiledLayer` в общую шину:
  `tile:heatrequested` (поставлен в очередь фонового прогрева —
  `TSCache.heat`), `tile:requested` (срочный запрос на показ — попадание
  в видимую область кадра), `tile:loaded` (успешно загружен/нарисован —
  единое событие для сетевых и generated/`StaticCanvasTileSource`
  тайлов), `tile:error` (не удалось после всех ретраев, `SRC-4`),
  `tile:outofview` (вышел из видимой области, но ещё в кеше/кольце
  прогрева), `tile:outofheat` (вышел за пределы кольца прогрева),
  `tile:evicted` (вытеснен из LRU-кеша, `LOAD-5`, полностью). Каждое — с
  `{layer, x, y, z}` в payload. **Зависит от: EVT-1. Оценка: M** (в
  основном разметка уже существующих точек кода — логика этих мест уже
  есть, добавляются вызовы `fire()`).
- [ ] **EVT-4** — Событие `resize` (`RESIZE-*`) и
  `layeradd`/`layerremove`/`layervisibilitychange` (`LAYER-*`). **Зависит
  от: EVT-1.**
- [ ] **EVT-5** — Миграция существующих `onLocate`/`onZoom`
  (единственные сегодняшние колбэки конструктора) на новую шину как
  тонкие обёртки поверх `on('move', ...)`/`on('zoom', ...)` — обратная
  совместимость самих опций конструктора сохраняется. **Зависит от:
  EVT-1, EVT-2. Оценка: S.**

## Фаза 9. Координаты и проекции (`PROJ-*`)

- [ ] **PROJ-1** — Модуль `mercator.ts`: чистые функции
  `lonLatToWorld(lon, lat)`/`worldToLonLat(v)`, параметризованные тем же
  `zoom_level_max`/`tile_size`, что уже использует тайловая адресация —
  без DOM-зависимостей, юнит-тестируется так же прямолинейно, как
  `Mat2D`. Фундамент для `VEC-3a`/`VEC-7` (реальные GeoJSON-данные),
  `DYN-*` (GPS-треки, `Geolocation`). **Оценка: S.**
- [ ] **PROJ-2** — Публичный API на `MapWidget`: `lonLatToWorld`/
  `worldToLonLat`/`screenToLonLat`/`lonLatToScreen`, поверх уже готовых
  `screenToWorld`/`worldToScreen`. **Зависит от: PROJ-1. Оценка: S.**
- [ ] **PROJ-3** — Поддержка альтернативных проекций (не только Web
  Mercator) через общий интерфейс `Projection` — не блокирует MVP,
  почти всем картографическим виджетам достаточно Web Mercator; задел
  на будущее. **Зависит от: PROJ-1. Оценка: M, приоритет низкий.**

## Фаза 10. Композиция слоёв и оверлеи (`LAYER-*`)

- [ ] **LAYER-1** — Менеджер слоёв на `MapWidget`: `addLayer`/
  `removeLayer`/`insertLayerBefore` вместо текущего фиксированного
  массива `ALL_LAYERS`, собираемого вручную в `src/index.ts` —
  управление порядком отрисовки в реальном времени, стреляет
  `layeradd`/`layerremove` (`EVT-4`). **Зависит от: EVT-1. Оценка:
  S-M.**
- [ ] **LAYER-2** — Прозрачность и смешивание на уровне слоя:
  `Layer.opacity` (`ctx.globalAlpha` вокруг всего `draw()` слоя целиком,
  а не только внутри отдельных `onTileDraw`, как местами сейчас) и
  `Layer.blendMode` (`ctx.globalCompositeOperation`) — нужно для
  полупрозрачных оверлеев, по прямому запросу. **Оценка: S.**
- [ ] **LAYER-3** — `FunctionLayer` — слой, рисующий себя произвольным
  колбэком `draw(ctx, map)` каждый кадр, без обращения к тайловому кешу
  или сети вообще — по прямому запросу ("оверлейные слои... в том числе
  формирующиеся динамически по событиям... рисуется заданной функцией,
  не ставится на закачку"). Простейший строительный блок для тепловых
  карт, живых оверлеев, дебаг-визуализаций. **Зависит от: LAYER-1.
  Оценка: S.**
- [ ] **LAYER-4** — Ленивая перерисовка оверлея по требованию:
  dirty-flag/`invalidate()` на слое, чтобы статичный оверлей не
  перерисовывался каждый кадр без необходимости — оптимизация, не
  блокирует `LAYER-3`. **Зависит от: LAYER-3. Оценка: S, приоритет
  низкий.**
- [ ] **LAYER-5** — `LayerGroup` — управление видимостью/порядком/
  прозрачностью пакета слоёв как одним целым (например, "весь трек" =
  точки + линия + подписи одной группой). **Зависит от: LAYER-1.
  Оценка: S.**
- [x] **LAYER-6 — сделано.** Атрибуты слоя по прямому запросу: вид слоя и границы
  тайловой пирамиды. Оба пункта — новые ОПЦИОНАЛЬНЫЕ поля на
  `Layer`/`TiledLayerOptions`, полностью обратно совместимые (по
  умолчанию — сегодняшнее поведение без изменений); существующий
  `TiledLayerOptions.z_max` (на самом деле смещение уровня, а не предел —
  см. многочисленные `// todo: rename to z_deep` в коде) **не
  переименовывается и не меняет семантику в этой задаче** — риск задеть
  каждого потребителя (`src/layers.ts`, потенциально параллельно
  редактируемый другой задачей) не стоит того, чтобы делать это заодно;
  переименование — отдельный маленький тикет на будущее.
  1. **Вид слоя** (`Layer.kind?: 'cartographic' | 'local'`, по умолчанию
     `'cartographic'` — сегодняшние реальные тайловые слои остаются как
     есть без правки конфигов): картографические слои опираются на общую
     мировую систему координат виджета (в перспективе — с привязкой к
     WGS84 через `PROJ-*`), произвольные (`'local'`, например XKCD) —
     нет. Они "несовместимы" не в смысле технического запрета рисовать
     оба разом (сегодня оба живут в одном мировом пространстве камеры и
     технически прекрасно сосуществуют, просто в разных его областях), а
     в смысле: не имеет смысла предлагать их как взаимоисключающие
     альтернативы одного "слота" подложки — `kind` даёт демо-панели
     (`DEMO-1`) и будущему `LAYER-1`-менеджеру данные, чтобы группировать
     базовые слои по видам отдельными радио-группами, а не одной общей.
     MVP — только само поле + геттер/проверка; никакого рантайм-запрета
     на одновременную видимость не вводится (это решение UI-уровня, не
     ядра).
  2. **Границы тайловой матрицы и глубина пирамиды**
     (`TiledLayerOptions.bounds?: {minX,minY,maxX,maxY}` в мировых
     единицах — тех же, что `map.c`/`Vector`, не lon/lat, `PROJ-*` для
     этого ещё нет — и `zLevelMin?`/`zLevelMax?: number`, отдельные
     новые имена, не путать с существующим `z_max`-смещением): это не
     просто документирующие поля, а реальное поведение —
     `TiledLayer.getLevelParams`/`draw` должны (а) зажимать вычисленный
     уровень `z` в `[zLevelMin, zLevelMax]`, когда они заданы (сейчас
     `z` растёт/падает без ограничений вместе с `zoom_factor`), (б)
     пропускать (не запрашивать у источника, не рисовать) тайлы за
     пределами `bounds`, если он задан — полезно и для XKCD (комикс
     занимает конечную область мира), и для любого регионального набора
     данных в будущем. Не задано — поведение как сегодня (без
     ограничений), обратная совместимость. Частично закрывает третий
     пункт ["Открытые вопросы"](#открытые-вопросы) (буст масштаба сверх
     нативного разрешения) — даёт слою способ заявить "глубже своего
     `zLevelMax` у источника данных нет", но сам `allowUpscale`
     (продолжать рисовать крупнее при упоре в `zLevelMax`, а не просто
     останавливаться) — отдельная, не эта задача.
  Тесты — юнит на `getLevelParams`/`draw` (map.test.ts): `zLevelMin`/
  `zLevelMax` реально зажимают `z`, `bounds` реально исключает тайлы вне
  области, оба поля отсутствуют → поведение не меняется (regression-тест
  на уже существующие сценарии). **Зависит от: AFF-4 (готово). Оценка: M.**

  **Ретроспектива.** Сделано субагентом, смержено вручную (не
  `git merge` — ветка была основана на коммите заметно позади текущего
  `master`, `git merge --no-ff` дал бы конфликты по всему файлу из-за
  разошедшейся истории, а не по содержимому; вместо этого — точечный
  `git diff <база агента> <результат агента> -- src/map.ts`, чисто
  применённый через `git apply`, плюс ручное слияние `src/map.test.ts`
  — его импорт-блок и хвост файла успели разойтись с `master` за счёт
  `ZOOM-12`/`ZOOM-13`, поэтому новые тесты агента дописаны в конец файла
  руками, а не патчем). Реализовано ровно по спеке: `Layer.kind` как
  метаданные (без рантайм-проверок), `TiledLayer.getLevelParams`
  зажимает уровень в `[zLevelMin, zLevelMax]` **после** применения
  существующего `z_max`-смещения (не вместо него), новый экспортируемый
  `computeTileIndexBounds()` переводит `bounds` из мировых единиц в
  индексы тайлов через собственную трансформацию слоя (тем же приёмом,
  что и `computeTileGridMatrix`), `TiledLayer.draw` пропускает тайлы вне
  этого диапазона, не дёргая ни `tile_source.get()`, ни отрисовку.
  `z_max` не тронут ни разу — ни имя, ни семантика, по прямому указанию
  в задаче (чтобы не создавать конфликт с параллельно шедшей
  `DEMO-1`/`DEMO-2`/`DEMO-4`/`DEMO-6`, которая в это же время правит
  `src/layers.ts`, где `z_max` стоит на каждом слое). Проверено
  независимо (свои числа, не те, что использовал субагент в юнит-тестах):
  через Playwright, динамическим `import()` живых собранных модулей
  прямо со страницы — `zLevelMin=3/zLevelMax=9` с `z_max=4` зажимают
  уровень `24 → 9`, не трогая случай `4` (внутри диапазона);
  `computeTileIndexBounds` с сдвигом `(-200, 50)` и мировым прямоугольником
  `[-200,50]..[120,434]` при `world_tile_edge=64` даёт ровно
  `{minX:0,minY:0,maxX:5,maxY:6}`; `TiledLayer.draw` с
  `bounds` на индексный диапазон `4×4` тайлов при заведомо большем
  видимом окне (`900×900`) реально запросил ровно 16 тайлов, ни одного
  за пределами диапазона. `make typecheck` — чисто; `make test-client`
  — **92/92** (было 83, `map.test.ts` вырос на 9 тестов); `make build`
  собрал `docs/js/map.js`. Консоль браузера — только сопутствующий
  404 фавикона.

## Фаза 11. Сериализация состояния (`STATE-*`)

- [ ] **STATE-1** — `MapWidget.serializeState()`/`deserializeState()` —
  чистые функции сериализации `{c, zoom_factor, rotation}` (плюс
  опционально активные слои) в компактный объект/строку, не привязанные
  к тому, КУДА пишется результат (URL, `localStorage`, отправка на
  сервер) — расширяет то, что уже частично сделано в `ROT-2`
  (URL-сериализация позиции+поворота по двойному клику), добавляя зум
  (сейчас теряется при сохранении в URL) и отвязывая сериализацию от
  URL конкретно. **Оценка: S.**
- [ ] **STATE-2** — Автоматическая, троттлированная запись в реальном
  времени (по прямому запросу), по событиям `move`/`zoom`/`rotate`
  (`EVT-2`, не на каждый кадр — `history.pushState`/`replaceState`
  каждый кадр анимации заметно тормозит и не нужен): в процессе —
  троттленный `replaceState` (не `pushState`, чтобы не заспамить историю
  браузера кнопкой "назад" простым панорамированием); по `moveend`/
  `zoomend`/`rotateend` — опционально `pushState`, если явно включено
  (не поведение по умолчанию). **Зависит от: STATE-1, EVT-2. Оценка:
  S-M.**
- [ ] **STATE-3** — Восстановление состояния при загрузке — уже частично
  есть (`src/index.ts` парсит `#[x,y,rot]`), перевести на общий
  `deserializeState()` и добавить зум в формат URL. Восстановление —
  мгновенное (`FLY-4`/`jumpTo`), не анимированный перелёт через весь
  мир. **Зависит от: STATE-1, FLY-4. Оценка: S.**
- [ ] **STATE-4** — Версионирование формата (короткий префикс/поле
  версии в сериализованном состоянии), чтобы будущее расширение формата
  (например, добавление активных слоёв) не ломало уже сохранённые/
  расшаренные ссылки. **Зависит от: STATE-1. Оценка: S.**

## Фаза 12. Плавные переходы вьюпорта (`FLY-*`)

- [ ] **FLY-1** — `map.flyTo({c, zoom, rotation, duration, easing})` —
  управляемый по времени (аналогично уже сделанному в `ZOOM-2`, не по
  кадрам) переход МЕЖДУ двумя полными состояниями вьюпорта разом.
  Сейчас `zoom_target`/`rotation_target` — независимые друг от друга
  "магниты", каждый едет своим темпом при простом присваивании; для
  согласованного скачка "от точки к точке" нужен единый прогресс 0..1 по
  заданной кривой, применяемый ко всем параметрам сразу — иначе зум и
  панорама доедут в разное время, и получится визуальное "рыскание".
  **Зависит от: EVT-1 (для событий ниже). Оценка: M.**
- [ ] **FLY-2** — События перехода: `flystart`/`fly` (каждый кадр, с
  текущим прогрессом 0..1)/`flyend`, и `flyinterrupt` — если
  пользовательский жест (wheel/drag/touch) прервал `flyTo` до
  завершения (обычный кейс — пользователь трогает карту руками посреди
  программной анимации; она должна вежливо остановиться, а не
  бороться с вводом). **Зависит от: FLY-1. Оценка: S.**
- [ ] **FLY-3** — Библиотека easing-кривых (linear/ease-in-out/
  cubic-bezier) — маленький чистый модуль, переиспользуемый и `FLY-1`, и
  потенциально существующим zoom/rotation-easing (`ZOOM-2`/`ROT-1`),
  если решат их когда-нибудь унифицировать (не обязательно сейчас).
  **Оценка: S.**
- [ ] **FLY-4** — `map.jumpTo(state)` — мгновенный (без анимации) аналог
  `flyTo`, для программной телепортации (`STATE-3` не должен пролетать
  через весь мир анимацией при восстановлении из URL). **Зависит от:
  FLY-1. Оценка: S** (в основном общая инфраструктура с FLY-1,
  `duration: 0`).

## Фаза 13. Адаптивный размер виджета (`RESIZE-*`)

- [ ] **RESIZE-1** — `ResizeObserver` на `container` вместо
  единственного текущего `window.onresize =` (перезаписывает, а не
  добавляет обработчик — конфликтует с любым другим кодом на странице,
  тоже слушающим resize окна) — реагирует на изменение размеров САМОГО
  контейнера (сайдбар свернулся, флекс/грид перестроились), не только
  окна целиком. **Оценка: S.**
- [ ] **RESIZE-2** — Событие `resize` (`EVT-4`) со старыми/новыми
  размерами. **Зависит от: RESIZE-1, EVT-1. Оценка: S.**
- [ ] **RESIZE-3** — Публичный метод `map.resize()` — принудительный
  пересчёт размера канваса без ожидания `ResizeObserver` (полезно сразу
  после программного изменения размеров контейнера, когда нежелательно
  ждать следующего кадра наблюдателя). **Зависит от: RESIZE-1. Оценка:
  S.**

## Фаза 14. Динамические и движущиеся объекты (`DYN-*`)

- [ ] **DYN-1** — `TrackLayer` (растущий трек): точки добавляются в
  реальном времени (например, от WebSocket/поллинга трекера), линия и
  текущая позиция перерисовываются по мере поступления, без пересоздания
  всего слоя на каждую новую точку. **Зависит от: VEC-1, PROJ-1. Оценка:
  M.**
- [ ] **DYN-2** — "Моя позиция" — слой-маркер, синхронизированный с
  `navigator.geolocation.watchPosition` (через `PROJ-2` для конвертации
  lon/lat), опционально с кругом точности (accuracy radius) и
  направлением (heading, если доступен). **Зависит от: DYN-1
  (переиспользует его механику точечного маркера), PROJ-2. Оценка: S.**
- [ ] **DYN-3** — Виртуальные движущиеся объекты по заданной траектории:
  параметризованное движение (путь — массив точек или параметрическая
  кривая; скорость постоянная или по контрольным точкам времени; easing
  между сегментами) — синтетический "живой" источник, отдельный от
  реальных трекеров, полезен для демо/тестов/симуляции нагрузки.
  **Зависит от: VEC-1, PROJ-1. Оценка: M.**
- [ ] **DYN-4** — Интерполяция/экстраполяция между редкими обновлениями
  трекера (dead reckoning): если новая точка трека приходит раз в N
  секунд, маркер должен плавно ехать к ней, а не телепортироваться —
  переиспользует `FLY-3`. **Зависит от: DYN-1, FLY-3. Оценка: S-M.**
- [ ] **DYN-5** — Общий "тик"-цикл для динамических слоёв,
  синхронизированный с `onRepaint` (не отдельные `setInterval` на каждый
  динамический объект) — единый источник `dt` для `DYN-3`/`DYN-4`.
  **Зависит от: DYN-3. Оценка: S.**

## Фаза 15. Закладки (`BOOKMARK-*`)

По прямому запросу: "механизм Закладок, чтобы можно было отмечать
интересные места и быстро на них переключаться". По сути — обобщение
уже существующего в демо прототипа (`locations` в `src/index.ts`,
захардкоженный объект с `pos`/`caption`/`go()`) в настоящий API ядра,
плюс возможность создавать закладки в рантайме, а не только руками в
коде.

- [x] **BOOKMARK-1 — сделано.** Новый модуль `src/bookmarks.ts` (по образцу
  `event_emitter.ts` — чистый, без DOM-зависимостей, отдельная от
  `MapWidget` иерархия, юнит-тестируется независимо): `interface
  Bookmark { id: string; name: string; position: Vector; zoom?: number;
  rotation?: number; layerId?: string }` и класс `BookmarkStore`
  (`add`/`remove`/`get`/`list`). Поле `layerId` — опциональное: если
  указано, переход к закладке дополнительно включает видимость этого
  слоя (`layer.visible = true`); не указано — закладка не привязана ни к
  какому слою и видна/применима независимо от того, какой слой сейчас
  активен (по прямому запросу: "если не указано — видна на всех
  слоях"). `MapWidget` получает поле `bookmarks: BookmarkStore`
  (создаётся в конструкторе) и метод `goToBookmark(id)` (пока — прямое
  присваивание `position`/`zoom_target`/`rotation_target`, как уже
  делает демо-шный `locations[...].go()`; переезд на анимированный
  `flyTo`/мгновенный `jumpTo` — позже, по готовности `FLY-1`/`FLY-4`, не
  блокирует MVP). Сериализация: `BookmarkStore.serialize()`/статический
  `deserialize()` в/из простого JSON-совместимого массива — САМ модуль
  не решает, куда это пишется (URL/`localStorage`/сервер), тем же
  принципом, что уже зафиксирован для `STATE-1` ("не привязано к тому,
  КУДА пишется результат") — конкретное хранение (`localStorage`) —
  дело демо-страницы (`DEMO_BACKLOG.md`/`DEMO-7`). **Зависит от:
  ничего блокирующего; естественно расширяется событиями
  `bookmarkadd`/`bookmarkremove` через `EVT-*`, когда та фаза дойдёт до
  сквозной инвентаризации событий — не блокирует MVP. Оценка: M.**

  **Ретроспектива.** Реализовано субагентом, чья ветка базировалась
  сильно позади текущего `master` (ещё до `LAYER-6` и всей серии
  `DEMO-1/2/3/4/6`) — диф по `src/map.ts` и `src/types/dat-gui.d.ts`
  применён патчем без изменений (эти файлы никто параллельно не
  трогал), а `src/index.ts` слит вручную (не патчем): у агента не было
  ни выпадающего списка базовых слоёв, ни хоткеев, ни атрибуции, ни
  закладки "Mandelbrot center" — логика закладок перенесена поверх
  актуального `index.ts` с сохранением всех этих более поздних
  добавлений, плюс сама точка Мандельброта заведена шестой seed-
  закладкой (её не было в исходном наборе агента). Привязка к слою — по
  `Layer.name` (единственное уже существующее идентифицирующее строковое
  поле; отдельного `id` на слоях в кодовой базе нет и не заводится этим
  тикетом). Проверено независимо (другие значения, чем у субагента):
  добавлена закладка с кириллическим именем, привязанная к
  `OpenTopoMap`, при активном ESRI — переход включил OpenTopoMap, не
  трогая ESRI (эксклюзивность — дело демо-UI, не `goToBookmark`),
  положение/зум/поворот восстановились точно; закладка пережила
  настоящую перезагрузку страницы (`localStorage`); переход по закладке
  без `layerId` ("Zero point") не тронул видимость ни одного слоя.
  `make typecheck`/`test-client` (**113/113**, было 101)/`build` —
  чисто.

## Сквозное. Инфраструктура и качество (`INFRA-*`)

- [x] **INFRA-1 — сделано, с поправкой к исходной формулировке.** Добавлен
  vitest (закреплён на `^3.2.7`, а не последней `5.x` — та требует Node
  ≥22, среда собрана на Node 18.19; отдельно стоит поправить/подтвердить
  при апгрейде окружения), `npm test`, цель `make test-client`. **По
  ходу выяснилась неточность исходной формулировки:** `heat`/`ring`/
  `square` в `src/tile_tree.ts` **не** тестируемы "без всякого
  рефакторинга" — это файл в глобальном стиле (без `import`/`export`,
  как и весь остальной `src/`, см. аудит выше), а vitest прогоняет тесты
  через реальные ES-модули: чтобы импортировать оттуда функцию, файл
  нужно сделать модулем, а это каскадом требует того же от всех, кто
  ссылается на его глобальные символы (`tile_source.ts` зовёт глобальную
  `heat()` из `tile_tree.ts` напрямую, и так далее по цепочке) — то есть
  на практике означает миграцию всего `src/` на модули разом, ровно то
  большое решение, которое сознательно отложено (см. `AFF-1`). Поэтому
  первым делом протестирован не `tile_tree.ts`, а новый `AFF-1`
  (`Mat2D`) — он написан сразу как ES-модуль и ни от чего глобального не
  зависит по значению. **Обновление из `AFF-3`:** момент, когда `src/`
  перешёл на модули, наступил там (не в `AFF-4`/`INFRA-5`, как
  предполагалось здесь) — и `heat`/`ring`/`square` тут же покрыты
  9 тестами в `src/tile_tree.test.ts` (границы `square`/`ring` через
  проверку координат по метрике Чебышева и отсутствие дублей; `heat` —
  через прослеженные вручную эталонные случаи, включая один с реальным
  разворотом по `z±1`, чтобы не тестировать функцию только на
  вырожденных `deep=0` сценариях).
- [ ] **INFRA-2** — Линтер: ESLint + `@typescript-eslint`, согласовать
  правила со стилем кода (много `// todo:`-комментариев прямо в коде —
  по мере закрытия соответствующих задач из этого бэклога переносить
  актуальные todo в него/issues, а не оставлять в коде россыпью).
  **Оценка: S.**
- [ ] **INFRA-3** — CI (GitHub Actions): `typecheck` + `test` + `build` на
  каждый push/PR в `trapwalker/flatcraft`. **Зависит от: INFRA-1. Оценка: S.**
- [ ] **INFRA-4** — Каталог `docs/adr/` для коротких архитектурных решений
  (пригодится для `AFF-6`, а также чтобы зафиксировать уже принятые решения
  по `ROT-3`/`VEC-3` из раздела "Принятые решения" в постоянной форме).
  **Оценка: S.**
- [ ] **INFRA-5 (решено: виджет — отдельная переиспользуемая
  библиотека)** — Разделить переиспользуемое ядро виджета и демо-обвязку:
  сейчас `src/index.ts`/`src/layers.ts`/`src/defines.ts` мешают в одном
  каталоге ядро (`Vector`, `MapWidget`, `Layer`, `TileSource`) со
  специфичным демо-контентом (roaddogs-источники, xkcd-дерево,
  `dat.gui`-панель). Раз виджет задуман как переиспользуемая библиотека
  (см. "Принятые решения"), это разделение стоит делать не "как повезёт
  по ходу роста", а осознанно и раньше — естественная точка сделать это
  первым проходом сразу после `AFF-3` (модульная система уже готова
  раньше, чем предполагалось при написании этого пункта — см. `AFF-3`;
  публичный API ещё небольшой, границу `core`/`demo` легко зафиксировать
  контрактом, а не распутывать переплетение постфактум). **Зависит от:
  AFF-3. Оценка: M.**
- [ ] **INFRA-6 (решено: виджет — отдельная переиспользуемая
  библиотека)** — Пакетирование `src/core/` как публикуемого пакета:
  `package.json` сейчас `"private": true` и заточен только на компиляцию
  демо-сайта (`tsc` → `docs/js`, без бандлинга, без `.d.ts`-экспорта
  наружу). Нужно: отдельная точка сборки библиотеки (ESM + опционально
  UMD-бандл через esbuild/rollup, не завязанная на `docs/js`-раскладку
  демо-сайта), генерация и публикация `.d.ts`, явно зафиксированная
  публичная поверхность API (что именно экспортируется из `src/core/` —
  `MapWidget`, `Layer`/`TiledLayer`/`VectorLayer`, `TileSource` и его
  подклассы из `SRC-2`, `Mat2D`/`Transform2D` из `AFF-*`), semver и
  минимальный README с публичным API. Публикация в npm-реестр — решение
  на отдельное обсуждение, когда API стабилизируется; сама
  инфраструктура сборки/типов должна быть готова заранее, чтобы
  добавление зависимости "снаружи" не требовало рефакторинга.
  **Зависит от: INFRA-5, SRC-6. Оценка: M.**

---

## Открытые вопросы

Развилки, по которым решение **ещё не принято** — нужно сначала сформулировать
концепцию (обсуждение по мере грумминга `INBOX.md`), прежде чем заводить
задачи на реализацию. Ниже — предварительный разбор с учётом уже принятых
решений/архитектуры, не финальный ответ.

- **Оверлейные UI-виджеты поверх карты (баннеры, стрелка/шкала компаса,
  спидометр и т.п.) — встраивать абстракции в сам виджет, или отдать
  разработчику-хосту рисовать поверх на уровне страницы?**

  Предварительный разбор: у задачи на самом деле два разных случая, которые
  стоит не путать. (1) Простые HTML/CSS-оверлеи (баннер, компас как
  повёрнутая CSS-стрелка, спидометр как HTML-гейдж) — рисуются вне канваса,
  почти бесплатны по производительности, и им нужны от виджета только (а)
  позиционированные DOM-точки привязки (углы канваса — тот же паттерн, что у
  Leaflet/MapLibre control-corners) и (б) события состояния вьюпорта
  (`EVT-*`: угол для компаса, скорость для спидометра — если считать её из
  `DYN-*`/GPS). (2) Оверлеи, которым нужно рисоваться ВНУТРИ канваса (для
  визуальной слитности с картой, или когда элементов слишком много для DOM) —
  для этого уже есть `LAYER-3` (`FunctionLayer`, рисуется произвольным
  колбэком без обращения к тайловому кешу) — отдельного решения не требуется.

  Предпочтительное (пока не окончательное) направление, по аналогии с
  Leaflet/MapLibre (SOLID — Single Responsibility: рендер карты отдельно от
  прикладных виджетов-гейджей) и с уже принятым решением "виджет — маленькая
  переиспользуемая библиотека" (`INFRA-5`/`INFRA-6`): ядро даёт МЕХАНИЗМ
  (DOM control-anchors + `EVT-*` + `LAYER-3`), но НЕ поставляет сами
  компас/спидометр/баннер как встроенные виджеты — это дело хост-приложения
  или отдельного компаньон-пакета/демо-примера (кандидат для
  `DEMO_BACKLOG.md` — построить компас/спидометр на демо-странице ИМЕННО
  через этот механизм, тем самым проверив, что API вообще достаточно). Если
  этот разбор устоится — оформится в отдельную небольшую фазу `CTRL-*`
  (control-anchors + опционально `L.Control`-подобный базовый класс), после
  `EVT-*`/`LAYER-3`.

- **Новые жесты (двойной тап, тройной свайп, тап вторым пальцем при
  удержании первого и т.д.) — нужна ли отдельная абстракция
  распознавания жестов?**

  Предварительный разбор: текущая тач-логика (`ZOOM-3`) — фиксированный,
  захардкоженный набор (1 палец = пан, 2 пальца = пан+пинч+поворот
  одновременно), интерпретируемый прямо внутри `touchstart`/`touchmove`/
  `touchend`. Добавление ещё одного-двух жестов так же, инлайном, — терпимо;
  за пределами этого комбинаторика (тайминги между тапами, различение
  "тап" от "начало другого жеста" по длительности/движению, конфликт
  "это двойной тап или начало пинча, который тут же прервали") быстро
  делает этот код нечитаемым и хрупким. Здесь есть проверенный прецедент —
  `UIGestureRecognizer` (iOS)/Hammer.js: каждый жест — независимый
  небольшой автомат состояний, слушающий тот же сырой поток
  событий, сам решающий "я распознан" и стреляющий своим именованным
  событием (через `EVT-*`) — а встроенные пан/пинч/поворот становятся
  просто первым, "предустановленным" набором таких распознавателей, а не
  особым случаем в общем обработчике. Честно говоря о цене: главная
  сложность такого подхода не в самих распознавателях, а в разрешении
  конфликтов между ОДНОВРЕМЕННО подходящими кандидатами (два
  распознавателя следят за одним и тем же 2-touch вводом — как решить,
  кто "выиграл") — этот механизм и в iOS-е (`requireGestureRecognizerToFail`
  и т.п.) — сам по себе не тривиальный. **Предложение:** не строить сразу
  общий фреймворк — начать с одного-двух жестов, которые заведомо не
  конфликтуют по таймингу с уже существующими (двойной тап — самый
  безопасный кандидат), и только если реально понадобится третий-четвёртый
  жест с пересекающимся вводом — тогда выделять `GEST-*` как
  надстройку над `EVT-*`.

- **Буст масштаба растровых тайлов сверх их нативного пиксельного
  разрешения (чтобы мелкие подписи/названия улиц оставались читаемы),
  по слоям.**

  Проверено (не просто предположение пользователя): да, сейчас это
  архитектурно невозможно в принципе, не просто "не настроено". `zoom_max`
  в `MapWidget` — **всегда ровно `1`**, безусловно, не per-layer
  (`src/map.ts`), и `k = zf / 2^z` (масштаб тайла относительно нативного
  размера, `TiledLayer.getLevelParams`) поэтому никогда не превышает `1` —
  структурно тайл физически не может быть отрисован крупнее своего
  нативного пиксельного размера. Как только `zf` растёт настолько, что
  k превысил бы 1, код просто уходит на следующий, более глубокий `z`
  (если он там есть на сервере) — ровно то поведение, которое описал
  пользователь. При этом в текущей конфигурации демо `zoom_level_max`
  (у `MapWidget`, `=18`) и `z_max` каждого слоя (у `TiledLayer`, тоже `=18`
  для OSM) совпадают ровно так, что `zoom_max=1` приходится точно на
  предел нативного разрешения источника — то есть "упереться в отдельные
  пиксели без возможности разглядеть подпись" наступает ровно на границе
  того, что вообще есть у сервера, и дальше идти некуда.

  Направление решения вырисовывается уже сейчас достаточно чётко (в
  отличие от двух вопросов выше) — тот же приём, что `maxNativeZoom` у
  Leaflet/nakarte (см. `DEMO_BACKLOG.md`/`DEMO-2`): у слоя должно быть
  понятие "глубже этого z у источника реальных тайлов нет" (отдельное от
  общего `zoom_level_max` виджета, который тогда нужно поднять выше общего
  знаменателя всех слоёв, чтобы вообще дать зуму запас хода) — и per-layer
  флаг (например `TiledLayerOptions.allowUpscale`), по которому
  `getLevelParams` при достижении этого предела перестаёт запрашивать
  более глубокий `z` (которого всё равно нет — получили бы 400, см.
  `LOAD-1`) и вместо этого удерживает `z` на месте, позволяя `k`
  расти дальше `1` — тот же код рисования, что и сейчас, только без
  верхнего клампа для тех слоёв, что явно об этом попросили. Не заводится
  в задачу прямо сейчас по прямой просьбе (сначала сформулировать
  концепцию для всех трёх вопросов en bloc) — кандидат на оформление в
  следующий свободный номер `ZOOM-*` на следующем груминге, если
  направление подтвердится (не привязываю здесь конкретную цифру —
  номер уже дважды съезжал из-за того, что более срочные тикеты
  забирали его раньше, см. `ZOOM-12`/`ZOOM-13`).

  **Обновление:** прямой запрос "у слоя должно быть в атрибутах указаны
  границы тайловой матрицы и глубина пирамиды" завёл половину этого
  направления в работу уже сейчас, не дожидаясь общего вывода по всем
  трём вопросам — см. `LAYER-6` (`zLevelMin`/`zLevelMax`/`bounds`).
  `LAYER-6` даёт слою заявить, ГДЕ у источника кончаются реальные
  данные — сам `allowUpscale` (рисовать крупнее нативного вместо того,
  чтобы просто останавливаться на границе) там сознательно не сделан,
  остаётся именно этим открытым вопросом до отдельного решения.

## Принятые решения

Развилки, по которым решение уже принято в обсуждении бэклога (закрывают
соответствующие пункты "Открытых вопросов" из более ранней версии файла):

- **Playwright настроен и проверен в этом окружении.** Готовый глобальный
  MCP-сервер `playwright` не подключался: Node в системе — 18.19.1, а
  `@playwright/mcp` требует 20+, плюс сам Chrome/Chromium не был
  установлен. Исправлено без затрагивания системного Node/apt: Node 20.19.5
  поставлен пользовательски в `~/.local/node20` (системный `nodejs` 18,
  от которого зависят другие apt-пакеты, не тронут), MCP-сервер
  перенастроен на этот Node и на `--browser chromium --headless
  --no-sandbox` (свой Chromium через `npx playwright install chromium` +
  `npx @playwright/mcp install-browser chrome-for-testing` — версия
  сборки должна совпадать с тем, что использует конкретная резолвнутая
  версия `@playwright/mcp@latest`). С тех пор используется для сквозной
  проверки виджета в реальном браузере (см. `AFF-3`) — навигация через
  локальный HTTP-сервер (`file://` заблокирован защитой playwright-mcp
  по умолчанию), проверка консоли/сетевых запросов, скриншоты, а также
  диспатч синтетических событий указателя для проверки точной числовой
  корректности (например, `screenToWorld` в `AFF-3`). Пригодилось и для
  визуальной проверки `ROT-3` (на практике оказалось, что там как раз
  **не** нужен буфер — см. ниже) и вообще всего, что нельзя проверить
  одним юнит-тестом.
- **ROT-3 — буфер не понадобился; решение пересмотрено после `AFF-4`.**
  Изначально планировался единый офф-скрин буфер под поворот (см. запись
  ниже — оставлена как историческая) — идея была в том, что на
  диагональных стыках тайлов при индивидуальном повороте видна щель
  (антиалиасинг края одного тайла не совпадает с антиалиасингом
  соседнего). План был написан до `AFF-4`. Когда до `ROT-3` дошли на
  практике, `AFF-4` уже рисовал все тайлы слоя через ОДНУ общую матрицу
  за кадр (`ctx.setTransform`) — а значит все тайлы одного слоя всегда
  поворачиваются идентичным преобразованием, и щели на стыках просто
  неоткуда взяться (та же причина, по которой их нет и без поворота).
  Проверено эмпирически на 23° и 45° — идеально бесшовно без всякого
  буфера. Реальная работа `ROT-3` свелась к тому, что и так было сделано
  в `ROT-1` (включить пользовательский ввод поворота камеры).
  <details><summary>Историческая запись — исходный (нереализованный) план буфера</summary>

  Единый офф-скрин буфер, а не поворот каждого тайла отдельно: рисование
  мозаики в буфер и поворот буфера целиком давал бы один шов только по
  внешнему периметру буфера. Дополнительный бонус: при чистом жесте
  поворота (без панорамирования/зума) буфер не пришлось бы перерисовывать
  вообще — только заново `drawImage`-ить под новым углом. Составляющие,
  которые обсуждались: запас по размеру буфера на диагональ, dirty-flag
  инвалидация, прогрев по площади буфера, финальное сглаживание.
  </details>
- **VEC-3 — сразу проектируем под тайловые векторные данные (MVT/vector
  tiles), а не только статический GeoJSON.** Задача разбита на `VEC-3a`
  (базовый контракт источника + простой статический адаптер — всё равно
  нужен для тестов/демо/нетайловых оверлеев), `VEC-3b` (сам MVT-декодер
  поверх общей `TilingScheme`) и `VEC-3c` (явное решение не сшивать
  геометрию на границах соседних тайлов — стандартная практика
  MapLibre/Mapbox GL, избавляет от отдельного проекта по сшивке). Следствия
  зафиксированы в `VEC-4` (hit-testing по границе тайла — принятое
  ограничение) и `VEC-5` (отдельный пространственный индекс нужен только
  нетайловому источнику `VEC-3a`, тайловый уже сам является индексом).
- **AFF-6 — отложено полностью, вне текущего бэклога.** Обсуждались два
  пути (честная перспективная математика на Canvas2D — или CSS3D
  `matrix3d` на отдельных DOM-слоях); предпочтительным считается первый
  вариант (единая система координат с `AFF-*`, вероятно поверх
  `computeTileGridMatrix`), но реализация не начинается сейчас и ни одна
  другая задача бэклога от неё не зависит. **Уточнение из `ROT-3`:**
  предполагавшийся "тот же буферный пайплайн, что и ROT-3" не появился —
  ROT-3 обошёлся без буфера; конкретный механизм для AFF-6, если до него
  дойдёт, нужно будет спроектировать заново.
- **Виджет — отдельная переиспользуемая библиотека**, не только часть
  этого демо-сайта. Следствия: `SRC-6` (декларативная конфигурация
  источников) обязательна, а не опциональна; `INFRA-5` (разделение
  `core`/`demo`) сдвинута раньше в дорожную карту — сразу после
  `AFF-1`..`AFF-4`, пока публичная поверхность API ещё небольшая; добавлена
  новая задача `INFRA-6` (пакетирование `core` как публикуемого пакета —
  бандл, `.d.ts`, semver, README с публичным API).
- **Порядок фаз подтверждён** как есть в разделе "Дорожная карта" выше:
  после Фазы 0 (быстрые правки) первым делом — афинное ядро (`AFF-*`),
  дальше остальные фазы в описанном порядке/параллельности.
- **Событийная модель (`EVT-*`) называется по образцу Leaflet/MapLibre**
  (`movestart`/`move`/`moveend`, `zoomstart`/`zoom`/`zoomend`,
  `map.on(type, handler)`), а не изобретается с нуля — это уже знакомый
  разработчикам, устоявшийся нейминг для ровно такого же набора понятий
  ("живое" состояние vs "конечное", отдельные события на старт/процесс/
  завершение). Собственный лёгкий emitter (`EVT-1`) — не внешняя
  зависимость (RxJS/EventEmitter3 и т.п.) — виджет задуман как
  переиспользуемая библиотека (см. ниже), лишний вес и чужой API
  подписки в паблик-серфейсе нежелательны ради того, что решается тремя
  методами.
- **Проекции (`PROJ-*`): сразу решаем только на Web Mercator, общий
  `Projection`-интерфейс — низкий приоритет, не MVP.** Абсолютное
  большинство картографических виджетов (Leaflet, MapLibre, Google/
  Yandex Maps) используют только Web Mercator; поддержка альтернативных
  проекций (`PROJ-3`) добавляет сложность, для которой сейчас нет
  конкретного сценария использования — заведено как задел, не
  блокирует `DYN-*`/реальные геоданные.
- **Сериализация состояния (`STATE-*`) — `serializeState`/
  `deserializeState` как чистые функции, не привязанные к URL.**
  Изначальная реализация (`ROT-2`) писала прямо в `location.hash`
  из `MapWidget.update_url_position()` — при появлении других мест
  хранения состояния (localStorage, отправка на сервер, синхронизация
  между несколькими виджетами на странице) это пришлось бы дублировать.
  Разделение на "чистая сериализация" + "куда писать" (`STATE-2`)
  решает это раз и навсегда. Троттленный `replaceState` во время
  движения, `pushState` — только по явному флагу на "конечных" событиях
  (`moveend`/`zoomend`/`rotateend`), чтобы не заспамить историю браузера
  обычным панорамированием (стандартная практика — так же делает
  большинство картографических SPA).
- **Плавные переходы (`FLY-*`) — отдельный механизм, не объединяется с
  существующим exponential-decay easing `zoom_factor`/`rotation` из
  `ZOOM-2`/`ROT-1`.** Тот механизм — "магнит", тянущий текущее значение
  к независимо изменяемой цели, без фиксированной длительности или
  событий начала/конца; `flyTo` — управляемый по времени переход между
  ДВУМЯ полными состояниями сразу (позиция+зум+поворот согласованно) с
  конкретной длительностью и кривой. Унификация этих двух механизмов
  технически возможна (`FLY-3`'s easing-библиотека спроектирована так,
  чтобы её можно было при желании переиспользовать и в `ZOOM-2`/`ROT-1`
  тоже), но не делается сейчас — два разных случая использования, риск
  сломать уже отлаженное поведение ради унификации ради унификации.
- **Редактирование геометрии (`VEC-8`) — базовый vertex-editor
  (хендлы на вершинах, драг, добавление/удаление по клику), не
  полноценный CAD-инструмент.** Undo/redo (`VEC-9`) — стек снапшотов
  геометрии поверх событий `VEC-8`, не общий command-pattern на весь
  виджет (мультипользовательское совместное редактирование, конфликты
  правок и т.п. — вне рамок этого бэклога, у виджета нет сетевого слоя
  синхронизации; хост-приложение само решает, как персистить/
  синхронизировать отредактированные геометрии, виджет только отдаёт
  чистые события).
- **Динамические объекты (`DYN-*`) — общий "тик"-цикл на `onRepaint`,
  не отдельные таймеры на каждый объект.** При множестве одновременно
  движущихся объектов (несколько трекеров + виртуальные объекты) N
  независимых `setInterval` хуже для батарейки/производительности и не
  синхронизированы друг с другом и с рендер-циклом; единый `dt` из
  `onRepaint` (`DYN-5`) даёт согласованное движение и переиспользует уже
  готовую инфраструктуру `frame_dt` (`ZOOM-4`/`ROT-1`'s continuous-hold
  navigation).

Все развилки из первоначального обсуждения (кроме трёх, ожидающих концепции —
см. [Открытые вопросы](#открытые-вопросы) выше) приняты и отражены в задачах
выше. Новые развилки/вопросы добавляются сюда/туда по мере появления, обычно
через грумминг `INBOX.md`.

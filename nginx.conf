# This file is documentation only — it is NOT the live nginx config.
#
# sublayers.net is served by a shared nginx (Docker Compose) on this host,
# managed outside this repo:
#
#   compose file:  /home/svp/nginx/docker-compose.yml
#   vhost config:  /home/svp/nginx/sites/sublayers.net.conf
#   container:     nginx-nginx-1 (image nginx:stable-alpine)
#
# Its `location /` root is bind-mounted straight from this repo's docs/
# directory (compiled output of src/*.ts — see ../Makefile / `make build`),
# so deploying is just: build, commit, done — nginx picks it up on the next
# request, no copy step or reload required. See docker-compose.yml's nginx
# service `volumes:` for the exact mount.
#
# (The stale version of this file used to describe a standalone vhost
# pointing at /home/svp/flatcraft/client/test_page — a path that no longer
# exists on this host; kept the filename for discoverability, replaced the
# content.)

// Ambient declaration for docs/js/full_xkcd_ts_data.js — a generated ~6MB
// data blob (quad-tree tile data), intentionally left as plain JS rather
// than converted to TypeScript: it carries no logic, only data, and
// round-tripping it through the compiler would add cost with no benefit.
declare const TILES_AS_TREE: Record<string, string>;
